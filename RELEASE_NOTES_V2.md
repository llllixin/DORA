# Dora · V2 Release Notes

## V2 · 真实业务分析（2026-09-07）

- C1 引擎覆盖补齐（c2 数据事件 / o2 门店集群机会，证据/语义/计数引擎化）
- C2 PostgreSQL Repository（Docker PG16 + 幂等 seed + 规则配置化，DB 离线 503）
- C3 真实文件上传入库（csv/xlsx 规范格式，事务替换，样例出厂重置）
- C4 上传列→口径映射（预览 + mapping 入库）
- C5 E2E 门禁（4 核心验收用例 + `tests/run_all`）+ 跌破阈值自动升级 e2
- 修复：V2 关键边界（空数据门禁 / 阈值语义函数化 / 时间戳动态化 / 数据源名称下沉）

> **⚠️ 范围脚注**：V2 版本的 **Watch/Action 域为静态演示数据**（引擎可计算并自动升级 e2，但业务目标委托、行动 case 的自动建档与真实执行仍为演示/静态），**真实动态委托功能将于 V4 版本上线**。V2 页面中"持续关注"计数旁有 `?` 悬浮提示标识该静态性。
