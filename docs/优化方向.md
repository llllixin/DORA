# Dora · 全流程完整性审计（2026-09-07 快照）

> ⚠️ **快照（截至 2026-09-07，不再更新）**：本文是当日全流程审计的记录，保留审计证据与发现。
> 处置状态已迁移：已处置项证据见 `docs/开发过程记录.md` 对应迭代；仍开放的候选/遗留/审查待办的**唯一维护点 = `docs/持续优化路线.md`**（§1 候选 / §2 遗留 / §5 审查遗留 / §6 顺序）。文档职责分层见 `docs/文档地图.md`。

> 审计范围：V1–V5 主线（数据→引擎→洞察→Reasoning(LLM)→Watch→Action→验证）全链路 + 工程质量与遗留双源。
> 审计证据：`tests/run_all` **7 段 ALL GREEN**（engine/ingest/reasoning/golden 9/e2e 4case/watch 4节/action 4节）；`npm run build` ✅；`openspec validate --specs --changes` 6/6 ✅；后端日志 0 Traceback；真实 DeepSeek 批量 refresh 9/9（4.9s）。

## 0. 完整性结论

**主线已经闭环且判定诚实**：业务数据（样例/上传）→ 确定性引擎判定洞察 → 模板/LLM 文案（数值锁）→ 变化进持续关注（Watch 委托+升级引用引擎）→ 问题/机会进行动档案（Case 建档→步骤→验证→归档，结果回写不污染判定）。
**尚未完成的是"记录驱动的边界外"**：真实外部动作执行、实时推送、分布式调度、多源/部署等（属设计内候选，不是缺陷）。

---

## 1. 已发现问题清单（审计发现，按处置分级）

### F 级 · 建议处置
| # | 问题 | 现状/影响 | 处置 |
|---|---|---|---|
| F1 | **静态双源仍在服务端**：`/api/actions*`、`/api/insights` 历史兜底（INSIGHTS）、`/api/evidence` 兜底（EVIDENCE）仍从 `app/data.py` 吐静态 | V4/V5 已真实化，但 e2e Case1/2 仍依赖旧 /actions 契约；易再养成"静态兜底"习惯 | 先改 e2e 与前端 sync 切新 /api/action/cases，再下线静态端点（保留 data.ts 仅作前端离线兜底） |
| F2 | **前端死代码**：doraApi `createFollowup`（旧 `/watch{insightId}` 语义已废弃）与 `executeAction` 无调用点 | 未来误用会 400 且语义误导 | 删除或迁移到新语义并注释 |
| F3 | **类型不诚实**：`listActionCases` 把"无 steps 的列表摘要"当 `ActionCaseDetail[]` 返回（TS 结构缺 steps） | 消费方需隐含"摘要"事实 | 拆 `ActionCaseCard`(摘要) 与 `ActionCaseDetail`(含 steps) 两个 DTO |
| F4 | **seed 档案会被样例重置**：`载入样例`→`run_seed` 用 upsert_seed_case 把 5 个静态档案复位到初始态 | 用户对 seed 档案的执行进度被工厂样例覆盖（自定义档案不受影响） | **✅ 迭代 35 修复（D032）**：seed 改"档案缺失才建档"，sample 不再归零已推进档案；测试自复位保证可重复 |
| F5 | **真实 LLM 路径无门禁**：run_all 钉死 template+空 key（正确），真实网络路径只靠手动冒烟 | 回归风险由 mock（数值锁/无 key fallback）兜底，真实调用不回归 | **✅ 迭代 35**：新增 `tests/llm_real_smoke.py`（无 key 自动 SKIP，不进 run_all）；实测单条 1.8s + 全量 9/9 |
| F6 | **Watch 周期调度无持久化队列**：单实例守护线程 60s tick，重启丢到期窗口（靠 last_checked 补跑） | 单实例可接受（D009 推迟 Redis/Celery），多进程部署前必须换 | **已记录为语义**（重启按 last_checked 补跑；多进程前换 Redis/Celery） |
| F7 | **前端无自动刷新**：Watch 列表/档案进度依赖跳页重挂载或按钮动作后的 reload；无轮询/SSE | 跨端变化需手动刷新才可见 | **✅ 迭代 35（轻量）**：Watch/Action 页 20s 轮询（真实模式）；SSE/正式推送留阶段 2 |
| F8 | **AskBar / FollowupCard「追问」仍是 mock**（V3 明确另立能力） | 用户点追问只见占位文案 | 候选保留（需规划后接 DeepSeek，见 `docs/持续优化路线.md` §1） |
| F9 | **数字一致性核对未做**（V2 遗留低优先）：引擎输出与页面展示逐字段核对 | 目前各视图均读引擎/DB 同源，风险低但仍需一次人工盘点 | **✅ 迭代 35**：摸底通过（9 insights evidence 全覆盖、Pulse counts=列表 counts）；**新发现单列 F11** |
| F10 | **env 文档小缺口** | 易误导新环境 | ✅ 已处置（迭代 34） |
| F11 | **洞察图表仍前端静态**（data.ts charts 按 id 硬编码，引擎数据未驱动） | 图表与判定同源但非引擎实时数据 | 候选（引擎驱动图表=新功能，见 `docs/持续优化路线.md` §2 遗留小项） |

### C 级 · 已知边界（设计内，不做不算缺陷）
- 判定一律引擎：LLM 只动文案；Watch escalate / Action resolved 均不伪造身份（P/D 红线）。
- `resolved` 需人工/工具 verify note；引擎指标恢复只作档案证据，不自动关闭（D031）。
- 支持范围显式：Watch 仅引擎词典指标、Action 仅当前引擎判定集——拒绝即诚实。
- urllib 单 socket 总超时 10s（D030）；单飞/熔断进程内有效（多进程前需集中状态）。

---

## 2. 候选与执行顺序（审计当时观点 —— 已迁移，不在此维护）

> 审计提出的候选与优先级（LLM 刷新阶段 2 / F9 一致性核对 / 可观测打点 / 工具执行层 / Redis-Celery / Docker / 多数据源 / Dora Chat / 生产部署）与建议执行顺序，**已全部迁移至 `docs/持续优化路线.md`**（§1 候选方向 / §2 遗留小项 / §6 推进顺序）。后续状态一律以持续优化路线为准。

## 3. 验收判据（归档时适用）

> 通用回归锚点已固化于 `docs/版本路线图.md` §3（质量锚点）与 `docs/持续优化路线.md` 头部规则 4；LLM 类优化"真实 DeepSeek 冒烟 + mock 覆盖降级"等要求随候选迁移至持续优化路线对应 change，不再在此重复。

## 4. 相关事实源（指针）

- 文档职责分层 / 一致性规则：`docs/文档地图.md`
- 候选 / backlog（唯一）：`docs/持续优化路线.md`
- 版本历史（V1–V5 归档）：`docs/历史版本路线.md`
- 问题 / 决策：`docs/开发问题与经验.md`
- 流程：`openspec/` + `.clinerules/project-conventions.md`
