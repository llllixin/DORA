# Dora · 持续优化路线（V5 后 backlog / 候选 · 唯一事实源）

> 2026-09-08 从 `docs/版本路线图.md` 拆出：V1–V5 已完成内容已归档到 `docs/历史版本路线.md`；本文收拢**全部未解决问题 / 候选方向 / 已选 backlog / 审查待办**。
> 规则：
> 1. 每行任务 = 一个 OpenSpec change（propose → 用户 review → apply → archive），勾选时附可复现验证；归档后同步本文状态 + dev-log 追加「迭代 N」+ 新经验/决策进 P/D。
> 2. 候选方向（§1）不占版本号；若升级为正式版本（V6 或其他）→ 先在本文末尾加版本规划块（范围/任务/接口/验收/进入条件），命名在规划时定。
> 3. 本文是**唯一 backlog**，不在其它文件复制清单。
> 4. 回归门禁基线：run_all ≥8 段 ALL GREEN、golden 9 不变、`npm run build` 绿、相关 curl（绕代理）+ 页面冒烟。

## 1. 候选方向（原路线图 §3C.6，不占版本号）

> 版本总数=5（搭建文档第 30–34 章）。V5 sign-off 后的工作属于"候选方向"，未入任何版本规划：
> 工具/Agent 执行层（真实外部动作，D031-1）、Redis/Celery 分布式调度与任务队列（D009 推迟项）、SSE/异步任务（LLM 兜底阶段 2）、多数据源/上传强化、Docker 全家桶（前端/后端/Redis 容器化，搭建文档 37 章）、生产部署（38 章）。
> 若用户决定把其中某一项升级为正式版本 → 先在本文件加版本规划块（范围/任务/接口/验收/进入条件）→ 再按序开工；命名（V6 或其他）在规划时定。
> 设计稿：工具/Agent 执行层的**类 Dify 工作流（多 Agent 会商 / RAG / 工具契约 / 可验证断言）** 见 `docs/workflow.md`；其首四项后端能力已于迭代 42 落地（`agent-tool-layer`：`GET /api/tools/query_metric`、`POST /api/knowledge/search`、`/api/agent/runs*`、`POST /api/dora/chat` SSE），Dify 配置见 workflow.md §11。

## 2. 遗留小项（来自已验收版本与审计，随时可做）

- [ ] 引擎结论与页面数字一致性核对（V2 遗留，低优先）：2026-09-07 F9 摸底已通过（9 insights evidence 全覆盖、Pulse counts=列表 counts）；剩余若已清则直接勾掉。
- [ ] F8 AskBar / FollowupCard「追问」真实化 = Dora Chat 候选：需规划后接 DeepSeek（另立，不夹带）。
- [ ] F11 洞察图表由引擎数据驱动：需后端 series/趋势导出端点（单列候选）。
- [ ] P011/D028 跟踪：LLM 兜底阶段 1 已完成；阶段 2（异步 job + 状态接口 + SSE）见 §1 候选。
- [ ] 可观测打点（P011 L4）：reasoning.llm.latency/errors/fallback/circuit、watch/action 事件数——为 LLM 阶段 2 与熔断退出条件提供数据。
- [ ] agent 栈剩余（workflow.md §6.2 未建项）：向量 RAG（embedding/pgvector）、job·SSE 队列（LLM 阶段 2）、Confidence Aggregator、真实工具执行（P2-7）。
- [x] 洞察页「为什么值得处理」区域展示收敛（2026-09-11 用户实测发现；本项同批覆盖本轮全部展示层改动 = change `insight-panel-polish`，UI 展示层 `skip_specs`，**迭代 43 归档**）：判断列复述下方「下一步建议」面板（同批 `semantics.next`）→ 去复述（归因行→左栏归因卡 / 下一步建议→下方面板 / 先例→左栏「历史先例」行）+ 左栏补「历史先例」行 + 两栏等高置信度贴底；同批并入 ①判断列接入 Dora AI 判断 `POST /api/dora/chat`（SSE，失败降级引擎 semantics + 兜底 note）②洞察列表折叠抽屉 ③归因卡两列 ④图表 `preserveAspectRatio="xMidYMid meet"` 等比 + 纵轴刻度按 `tickStep` 定小数位 ⑤脉搏页专家团 chips 可点 + 信号板/卡片收口（去掉「持续关注」卡「?」）。验证：`npm run build` ✅；`engine_check` ✅ + `run_all` **ALL GREEN**（golden 9 不变，后端零改动）；CDP 三 tab 判断列无复述 `[PASS]`、降级路径 6/6 `[PASS]`、抽屉 300→46、两栏 496×239 等高、三视口圆点宽高比 1.0、纵轴 6 刻度唯一；截图 `panel/judge/drawer_closed/pulse_chips/offline_fallback.png`。
- [x] 行动回路页收口（2026-09-11 用户提出；change `action-loop-timeline`，**迭代 44 已归档** `changes/archive/2026-09-11-action-loop-timeline`）：① **档案列表对齐洞察列表形态**（Tag(问题/机会) + 状态 pill + 编号 → 标题 → 来源 → 状态行；执行中在前、头部计数），状态做成 tag 并**二值化**「执行中 / 已归档」（open|running|waiting_verify→执行中、resolved→已归档，**仅展示层映射，状态机/接口不变**）② 「**学习经验**」从档案列表最下方移入知识库（经验唯一落点；知识库条目处理过程改可展开收起，去掉假按钮）③ 机会/问题板块步骤区改**时间线**：每步可展开收起（默认当前步）、推进控件右置、**专家团落到每一步**（per-step、可添加/移除、**后端持久化** `action_step.experts` + `POST /api/action/cases/{id}/steps/{seq}/experts`）、每步可「定位数据」（复用 `step.evidence` + 既有证据链）。验证：`action_check` 第 1–7 节全绿（新节「步骤负责专家：往返/去重/幂等/整体替换/上限/冻结/404」）、`run_all` **ALL GREEN**（8 段、golden 9 不变）、`npm run build` ✅、CDP 在线 **28/28 PASS** + 离线演示 **4/4 PASS**；spec 增量（1 ADDED + 3 MODIFIED）已合入 `openspec/specs/business-action/spec.md`（`openspec validate --specs` 7 passed）。复用死 CSS / 幂等端点 / 不预置专家 / 离线不伪造 等取舍见 `docs/开发问题与经验.md` D041。
- [ ] 前端死 CSS 规则清理（P013 顺带发现）：`frontend/src/styles.css` 的 `.facts{display:grid;...margin-top:9px}` 已无任何 className 使用（原 hero facts 网格遗留），与 `.judge-col` 撞名曾导致等高失效；建议扫描同类死规则后单列清理。
- [x] 洞察判断结构化（2026-09-11 用户提出；change `insight-judgment-structure`，**迭代 46 已归档** `changes/archive/2026-09-11-insight-judgment-structure`）：判断列由「一段散文」改为**三段式**——① **严重等级**由引擎按同一份快照显式判级（`SEVERITY_WEIGHTS`：`base`(50/45/25) + `max(偏离幅度, 距阈值)` + 持续 `min(n,6)×1.5` + 影响面 `min(n,4)×2` + 归因面 `min(n,3)×2`，切点 `≥75 high / 55–74 medium / else low`），下发 `severity{level,score,rule,drivers,basis}`（`rule` 自陈「引擎启发式」+ 逐项依据；`drivers` 数字全部可回溯）；② **为什么发生**沿用 chat 散文（`x.question`，不改 why 句式，避免被 `belongsElsewhere` 抽空）；③ **可能后果**由引擎确定性外推 `consequence{summary,horizon,condition,impacts,basis}`（`summary` 一律以「若…」开头 + 强制前提/观察窗口；证据不足的 `c2` 输出定性后果、不造数字）。配套：chat 新增 `consequence` 意图（`严重等级：`/`可能后果：` 两段固定前缀，排在 `handle` 之前以避开「不处理」含「处理」）、`InsightSummary` 增两字段（`GET /api/insights/{id}` 不再丢字段）、`belongsElsewhere` 增两条前缀（展开也不复述）、`data.ts` 离线镜像 9 条按引擎同口径补值（`engineJudgment` 常量表 + 挂载）。验证：`engine_check` ✅（新契约 + 数字锁回溯 + 9 条等级分布 + `c1 ≤ e2` 注入对照 + 敏感性/确定性；`pulse 3/2/4/4` 不变）、`agent_check` 第 4 节 ✅（后果两段 + why/evidence 不回归 + 数字锁）、`run_all` **ALL GREEN**（8 段、`golden 9` 不变）、`npx tsc -b` ✅、`npm run build` ✅、**CDP `/tmp/dora_judge3_verify.mjs` 22/22 PASS**（三 tab 三段齐全 / 去复述 / 两栏等高 + 置信度贴底 / 图表等比 / 抽屉 46↔300 / 1440·1024·390 无溢出 / 离线 AI 不可用三段仍在；截图 `/tmp/dora_shots3/judge3_{panel,cols,offline}.png`）、镜像「同名同值」比对 **0 处不一致**；spec 增量（`business-engine` ×2 + `business-agent` ×1，全 ADDED）合入 `openspec/specs/`（`openspec validate --specs` 7 passed）。判级/外推取舍、退出条件与 5 条 apply 期修订见 `docs/开发问题与经验.md` D040 + P016。
- [ ] 离线镜像（`frontend/src/data.ts`）数值与引擎口径同步（2026-09-11 迭代 46 apply 期发现）：镜像 9 条的 `metric/delta/desc/trigger/semantics` 仍是更早的演示快照（p1：`18.7% / ↓2.1% / 连续 3 天`），而本轮补入的 `severity/consequence` 按 D4 取自当前引擎（`18.1% / ↓5.4% / 连续 4 天`）→ 离线态同一张卡片会同时出现两套数字（判级依据 5.4% vs 关键数值 2.1%）。两个方向任选其一：① 把镜像数值整体同步到当前引擎口径（一次性 + 加脚本守）；② 离线态显式隐藏段①/段③并标注「离线 = 旧快照」（= D040 退出条件 ③）。
- [x] 持续关注（业务目标委托）页收口（2026-09-11 用户提出；change `watch-delegate-flow`，**迭代 45 已归档** `changes/archive/2026-09-11-watch-delegate-flow`）：① 右侧「Dora 接到委托后会做什么」由写死静态四步改为**真实状态驱动的流程**（待命 → 理解目标/匹配数据口径 → 持续检查或已暂停 → 命中回脉搏/引擎判定升级；派生逻辑 `features/watch/delegateFlow.ts::buildDelegateFlow` 纯函数，组件只渲染）；② 表格「操作」列 `0.45fr` → 固定 `208px`，三按钮同排（移动端操作区独占一行左对齐）；③ 离线回退时面板只推进步骤 1/2 并显式「需连接后端」+ 页头「离线演示」。为支撑 ①，`_watch_card` 增量回显 `intent`/`lastCheckedAt`/`lastEvent`（纯增量 camelCase，既有 9 键取值不变）。验证：`watch_check`（第 4 节新增卡片字段断言）✅、`run_all` **ALL GREEN**（8 段、golden 9 不变）✅、`npm run build` ✅、`npx tsc -b` ✅、`delegateFlow` 七态 **24/24 PASS**、CDP 在线全流程（待命→已理解待确认→命中→升级→暂停/恢复→删除）与 20s 轮询一致性 `[PASS]`、离线拦截 `/api/watch*` 三处文本 `[PASS]`、四视口（1440/1024/760/390）三按钮同排 + 无横向溢出 `[PASS]`（截图 `/tmp/watch-shots/watch-{1440,1024,390}.png`）；spec delta（1 MODIFIED）已合入 `openspec/specs/business-watch/spec.md`（`openspec validate --specs` 7 passed）。取舍与 apply 期修订见 `docs/开发问题与经验.md` D042 + P015。
- [ ] 行动回路「每步负责专家团」语义修正（2026-09-11 用户发现：步骤 1/2 由引擎自动完成，专家团该由系统指派；**方向已定 A 双轨，待 propose**）：① 引擎自动完成的步骤（`backend/app/action/builder.py::_steps_for` 建档即写死 `seq0=done` / `seq1=in_progress`）改标「系统自动执行 · 无需指派」，去掉「未指定负责专家」空态与指派入口；② 仅对留给人做的步骤（`seq2/3`）把档案级系统建议（`ORCH_DEFAULTS`）预填为默认负责专家——**落库并带来源标记**（`system|user`，用户改动记 `user`、可一键恢复系统建议），以消解 D041 决策 5 要防的「引擎镜像 vs 用户责任」双源；③ **真·权威指派仍等 E1**：`expert_registry` + supervisor 出 `team[] ⊆ expert_registry`（`docs/workflow.md` §4.1/步骤 6）落地后由后端按 `kind`/`scope` 指派，前端只读展示。→ change `action-step-expert-assign`（暂定 id；`business-action` spec delta，不 `skip_specs`；推翻/收窄 D041 决策 5 需同步 D 系列）。

## 3. 已选 backlog · 3D.1 前端接线收口（change 前缀 `frontend-loop`，W1–W3）

> 两块关系：**3D.2-E1（专家注册表）** 是 3D.1-W3「专家能力同源展示」与 3D.2-E2/E3 的前置；3D.1 其余项可与 3D.2-E1 并行。

### 3D.1 前端接线收口（change 前缀 `frontend-loop`）

> 定位一句话：消灭「页面写死业务判断 / 占位动作 / 静默 toast」，让 Pulse/Insight/Sidebar/Topbar/EvidenceDrawer 的**呈现与动作全部连到后端真实数据，或明确禁用+标注原因**；data.ts 只保留「离线演示」兜底语义。
> 现状证据（2026-09-08 审计）：Action/Watch/Knowledge/洞察列表/证据链/上传/重新解释已真连；剩余占位集中在 Pulse 子面板、Insight 页静态回退字典、Sidebar/Topbar/EvidenceDrawer 固定入口、AskBar/FollowupCard 追问（F8，另立）。

**范围（In scope）**
- Pulse 页真实化：Summary/优先级主区/问题栈/信号板/监控卡全部读 `/pulse` + `/insights` + `/watch`（引擎同源）；删除组件内写死的 view/signal/monitor/信号文案字典，文案由 desc/semantics/evidence 字段拼接。
- Insight 页静态回退字典下线：cause/next/n 只从 `semantics`（reasoning 下发）取；无 semantics 时显示「模板未覆盖」而非编造数字。
- Pulse「让 Dora 帮我跟进」→ 真实建 Watch 委托（p1 利润率 → `关注利润率…`）；按钮态与结果回写跟随 `/watch`。
- 框架接线：Topbar 证据入口跟随当前洞察（不再固定 p1）；Sidebar「Dora发现 n」读 `/pulse` 计数；EvidenceDrawer「定位原始数据」连 `/evidence/{id}`（path 不可定位则诚实禁用）。
- 离线边界显式化：detectApi 失败 → 顶栏「离线演示」标识；动作按钮要么真实调用、要么明确提示本地兜底，杜绝静默成功。
- 轮询补齐：Pulse/Insight 纳入 20s 轻量轮询（与 Watch/Action 一致，F7 语义）。

**非目标（Out of scope）**
- AskBar/FollowupCard「追问」真实化（F8 = Dora Chat 候选，另立规划，不夹带）。
- 图表由引擎数据驱动（F11）：需后端 series/趋势导出端点，单列候选。
- Action/Watch/Knowledge 域行为改动；专家注册表/编排编辑（归 3D.2）。

**接口（复用现有，预计零新增端点）**
```text
GET  /api/pulse            # ⚠ 仅纯计数（Summary）；watching=changes 计数 ≠ 真实委托数 → 监控/跟进计数不用它
GET  /api/insights?type=   # problem/opportunity/change（含 semantics/reasonSource）→ 主区/信号板字段来源
GET  /api/watch            # 监控卡与「持续关注」卡
GET  /api/evidence/{id}    # EvidenceDrawer 定位
POST /api/watch            # Pulse「让 Dora 跟进」建档（解析失败→显式 400 提示）
GET  /api/knowledge        # Sidebar 计数（可选）
```
> W1（frontend-loop-pulse，2026-09-08）已确认：Pulse 面板数据走 `/api/insights`+`/api/watch` 的 D008 同步结构，不消费 `/api/pulse`；**W3 Sidebar/Topbar 计数同样不要用 `/api/pulse.watching`**（design D1/D6、任务 5.5 归档同步）。

**任务（每项 = 一个 OpenSpec change，按序开工）**
| 任务 | 内容 | 输出/验证 |
|---|---|---|
| W1 `frontend-loop-pulse` ✅（2026-09-09 归档 `2026-09-09-frontend-loop-pulse`，迭代 39） | Pulse 面板数据全读后端（summary/信号板/监控卡/主区文案），删组件写死字典；「让 Dora 跟进」→ 真实 Watch 建档 | run_all ALL GREEN / build 绿 / 静态 grep 无命中 / 人工双态冒烟通过（详见归档 tasks + dev-log 迭代 39） |
| W2 `frontend-loop-insight` ✅（2026-09-09 归档 `2026-09-09-frontend-loop-insight`，迭代 41） | InsightPage 静态回退字典下线（next/cause/n 只用 semantics/引擎字段）；`data.ts` 离线镜像 9/9 补齐 | build 绿；无 semantics 显式「模板未覆盖」；run_all ALL GREEN；人工双态冒烟通过（见 dev-log 迭代 41） |
| W3 `frontend-loop-frame` | Topbar/Sidebar 计数真读 / EvidenceDrawer 定位 / 离线 banner / Pulse·Insight 20s 轮询补齐；Sidebar「专家团」CapabilityPanel 同源展示（依赖 3D.2-E1，可分两步落地） | build 绿；curl 计数与页面一致；离线回退有明确标识 |

**红线与验收计划**
1. 判断/数字不残留页面（D004/文档 42 章延续）：所有业务数字标注后端字段来源；data.ts 组件引用处注明「离线演示」。
2. 动作按钮三态：真实调用 / 明确禁用+原因 / 诚实提示离线，杜绝静默 toast（F 系列语义不回头）。
3. 回归锚点：run_all ≥7 段 ALL GREEN、golden 9 不变、`npm run build` 绿、页面冒烟（后端在线=真数据、离线=演示+标识）。
4. AskBar 追问若被要求接通 → 停下另立 F8 规划，不夹带进本块。

**进入条件**：V1–V5 门禁全绿（当前已满足）。W1 apply 前先用 `curl /api/watch/parse` 实测「利润率/margin」委托可解析（词典缺则 W1 先补词典再接线）；W3 中专家能力同源子项依赖 3D.2-E1 可用。

**补强要求（2026-09-08 审查，apply 前必须并入 proposal）**
1. W1/W2 删组件写死字典时，必须把等价内容迁移进 `data.ts` 离线镜像，并把「关后端离线冒烟 = 与现状 demo 观感一致」写进验收（否则删字典 = 离线演示空壳）。
2. W1 propose 先做「所需字段 ↔ 现有端点」盘点并明确「模板未覆盖 / 离线」两种降级文案：`/api/pulse` 现仅返回计数，不携带主区/信号板/监控卡所需全部字段。

## 4. 已选 backlog · 3D.2 专家团领域化（change 前缀 `expert-registry`，E1–E3，多 agent 前置）

### 3D.2 专家团领域化（change 前缀 `expert-registry`，多 agent 前置）

> 定位一句话：把「专家」从字符串标签（`orchestration.experts`）升级为**专家注册表实体**——id/角色/适用域/口径范围/能力描述/未来 agent 挂载位；建档引擎按 kind+metric 自动组队，Action 页可人工增补，Pulse/Sidebar 同源展示。为候选方向 §1「P2-7 工具/Agent 执行层」提供挂载底座（D031-1 预留口），红线延续 D009/D029/D031。
> 现状证据：专家散在 4 处且只是名字（`data.ts capabilities.专家团` / `autoExpertNotes` / `app/action/builder.py ORCH_DEFAULTS` / seed 静态档案 orchestration），无实体、无能力描述、Action 页只读「系统已选」不可增补。

**范围（In scope）**
- 域对象 + 持久化：新增 `expert` 表（expert_id、name、role、适用 kind=problem|opportunity|all、scope 指标口径、abilities、desc、agent 挂载字段可空）+ Repository CRUD + 幂等 seed（内置 ≥10 位：经营分析/财务/销售/门店运营/供应链/会员运营/经营增长/商品/数据分析等，覆盖现 5 档案 6 类引用并扩充）；新建能力 spec `business-expert`。
- 建档引擎自动选团：`builder.ORCH_DEFAULTS` 静态常量 → 按 kind+命中 metric 查 registry（无命中回退 kind 默认组，幂等）；seed 5 档案 orchestration 保留原值不改观感。
- Case 编排 API：专家只读列表 + 推荐团 + 编排回写（校验专家存在；只写 orchestration 元数据，**不回写判定/不伪造执行**）。
- 前端：Action 档案「专家团」chips 可增删（选择器读 registry，区分「系统已选/我增补」）；Pulse「专家团能力说明」与 Sidebar CapabilityPanel 读同一 registry（消灭 data.ts 双源；data.ts 仅保留标注「离线镜像」的兜底）。

**非目标（Out of scope）**
- 真实 agent 运行时/工具调用/自动执行（P2-7：需本块 sign-off 后另立版本规划；专家只提供挂载位，agent 字段恒 null 直到 P2-7）。
- AskBar/Chat 追问（F8）；Watch/Action 判定语义改动。
- 编排「自动执行步骤」：仍人/工具回填 note（D031 红线）。

**接口（新增，契约草案）**
```text
GET  /api/experts                       → { ok, experts:[{ id,name,role,kind,scope[],abilities[],desc,agent }] }   # 只读目录
GET  /api/experts/suggest?kind=&metric= → { ok, experts:[...] }            # 建档/详情页推荐组（builder 同源算法）
PUT  /api/action/cases/{id}/orchestration { experts:[id,...], data:[...] } # 校验专家存在；非法 400；回写持久化
GET  /api/action/cases/{id}             → orchestration 内嵌专家 id+name+role（兼容现前端结构）
```

**任务（每项 = 一个 OpenSpec change，按序开工）**
| 任务 | 内容 | 输出/验证 |
|---|---|---|
| E1 `expert-registry-domain` | expert 表 + Repository CRUD + seed 目录（≥10）+ `GET /api/experts` + 新建能力 spec `business-expert` + 归档索引/README | 空表 CRUD/幂等 seed；curl /api/experts 断言 ≥10 位含 id/role/kind/agent=null；run_all 保持绿（新增 expert_check 段） |
| E2 `expert-registry-orchestration` | builder 静态 ORCH_DEFAULTS → registry 建议组（kind+metric）；`PUT …/orchestration` 校验/回写；seed 5 档案编排不变 | 断言 builder 不再引用 ORCH_DEFAULTS 常量；PUT 非法专家 400/合法回写持久化；action_check/e2e 不回归 |
| E3 `expert-registry-ui` | Action 专家 chips 可增删；Pulse/Sidebar 能力说明同源展示；档案详情显示角色/范围 | `npm run build` 绿；页面冒烟（后端在线=registry、离线=镜像标注）；curl 编排读写一致 |

**红线与验收计划**
1. 专家只描述「谁适合处理什么」（orchestration 元数据），不持有判定权、不回写引擎判定/reasonSource（V3–V5 红线延续）。
2. agent 挂载位默认 null，UI/API 不暗示已可自动执行（诚实边界，D031-1/P2-7 之前）。
3. 新增专家 scope 建议属于引擎支持指标口径（D029 语义），不满足则标注「展示用」，不假装可调度。
4. 单一事实源：Pulse/Sidebar/Action 专家内容只读 registry；data.ts 仅离线镜像（注明）。
5. 回归锚点：run_all ≥8 段（新增 expert_check）ALL GREEN、golden 9 不变、`npm run build` 绿。

**进入条件**：V1–V5 门禁全绿（已满足）。E1 开工即走 opsx-propose（首个归档新建能力 spec `business-expert`）；E2/E3 依赖 E1；E3 的 Pulse/Sidebar 同源子项同时解锁 3D.1-W3 对应子项。P2-7 工具/Agent 执行层需本块 sign-off 后另立版本规划再开工。

**补强要求（2026-09-08 审查，apply 前必须并入 proposal）**
1. E2 明确历史 seed 5 档案与新档案的两套编排一致性策略（建议同 kind+metric 稳定输出专家组），并加 builder 断言。
2. knowledge 的 `change` 类型在出现真实写入方前，前端可去掉该恒 0 chip 或写明未来写入时机。

## 5. 审查遗留待办（2026-09-08 代码审查发现，按组跟踪）

| # | 问题 | 建议处置（change） | 优先级 |
|---|---|---|---|
| A1 ✅（2026-09-09 归档） | knowledge_archive / case_lesson 不随 action_case 生命周期联动（删除/基线恢复残留孤儿行；演示库已有 o2 矛盾实例） | `knowledge-lifecycle`：级联清理（同事务）+ action_check 不变式断言 ✅（已实现并验证，迭代 40） | 中 |
| A2 ✅（2026-09-09 归档） | verify(resolved) 与知识入库是两次 commit，异常窗口丢行且无补偿 | 并入 `knowledge-lifecycle`（合并单事务或幂等补偿 + 测试）✅（已实现单事务，迭代 40） | 中 |
| A3 ✅（2026-09-09 归档） | ActionCase.archive VARCHAR(500) 与每次 verify 追加冲突（多轮 continue / 长 note 会 PG 报错） | 并入 `knowledge-lifecycle` 或独立小 change：列改 TEXT + 追加策略 ✅（已扩 TEXT+幂等 ALTER，迭代 40） | 低-中 |
| B1 ✅（2026-09-09 归档） | tests/run_all.py STEPS 硬编码 "python3"，环境不同即全红 | 改 `sys.executable`；AGENT 写明依赖解释器 / venv ✅（已改，迭代 40） | 低 |
| C1 ✅（2026-09-09） | AGENT.md 多处与实况漂移（迭代 / D 编号 / spec 数 / 段数 / 文件指路） | docs 拆分 + 本次治理提交已一并刷新；后续由 `tests.check_docs` + 本文档地图防再漂 | 中 |
| C2/C3 ✅（2026-09-09 docs 治理提交） | 路线图遗留勾选与 F9 冲突；RELEASE_NOTES / V3–V5 验收清单等历史快照未标注「截至」日期 | C2 随拆分勾选迁移解决；C3 快照标注已补（RELEASE_NOTES_V2–V5 + V3–V5 验收清单头部） | 低 |
| D4 | knowledge `change` 类型空态 UI | 见 §4 补强 2 | 低 |

## 6. 推进顺序

1. **Phase 0（收口）✅（2026-09-09 归档 `2026-09-09-knowledge-lifecycle`，迭代 40）**：A1–A3 合并为 `knowledge-lifecycle` 归档 → 清理演示库 o2/p2 孤儿残留 → B1 一并提交；docs 拆分同步提交。
2. **Phase 1 ✅**：§3 W1（迭代 39）/ W2（迭代 41）均已归档；**Agent 首四项后端能力 ✅（迭代 42 `agent-tool-layer`）**；下一步 = §4 E1 专家注册表与 §3 W3。
3. **Phase 2**：§4 E1 → E2 → W3 / E3 汇合；回归锚点 ≥8 段（新增 expert_check）。
4. **Phase 3**：回到 §1 候选（LLM 阶段 2 / P2-7 工具层 / Docker / Chat 等）。
