# Dora · 开发问题与经验

> 用途：沉淀开发中遇到的问题、定位过程、解决方案、经验教训，以及**关键决策与动机**。
> 对应文档：《开发过程记录.md》记录功能与文件改动。
> 约定：
> - 新问题**追加在文末 P 系列**，编号 `P0xx`，经验教训单独提炼成一句"原则"。
> - 新决策**追加在文末 D 系列**，编号 `D0xx`，按"时间 / 决策 / 为什么 / 影响与代价 / 状态"记录——目的是让后人能看懂"为什么当初这么干、什么条件下可以推翻"。
> - **关键设计取舍须即时落档**：propose/design 阶段产出的取舍（技术选型、范围边界、备选否决）立即写 D 系列，不等 apply/archive——它是本文件的"复查与学习"材料，不能只躺在 design.md 里。

---

## P001｜打开 .tsx 满屏红波浪线 / Cannot find module

- **现象**：VS Code 打开任意 `.tsx` 都报大量 `Cannot find module 'react'` 之类错误，几乎每个文件都红
- **原因**：依赖从未安装，`node_modules` 不存在；TypeScript 语言服务读不到第三方包的类型声明，连锁报错
- **解决**：`cd frontend && npm install`；若装完后编辑器红线未消失 → `TypeScript: Restart TS Server` 或 Reload Window
- **教训 / 原则**：拿到任何前端工程（生成/克隆）第一步先确认 `node_modules`；"满屏红"先查"是不是没装依赖"，别先怀疑代码

---

## P002｜`Property 'env' does not exist on type 'ImportMeta'`

- **现象**：装完依赖后类型检查仍剩 1 个错：`doraApi.ts` 使用 `import.meta.env.VITE_API_BASE_URL` 报类型不存在
- **原因**：项目缺 Vite 标配的类型声明文件 `src/vite-env.d.ts`，TS 不识别 `import.meta.env`
- **解决**：新建 `frontend/src/vite-env.d.ts`，内容一行 `/// <reference types="vite/client" />`
- **教训 / 原则**：`import.meta.env`、`*.css` 模块等 Vite 特性需要类型声明支撑；看到"类型不存在"先想"是不是少了类型声明文件/依赖"，可能是工程缺文件而不是代码错

---

## P003｜`npm run build` 卡在 `tsc -b`

- **现象**：构建在类型检查阶段就中断，Vite 打包根本没跑
- **原因**：package.json 的 build 脚本是 `tsc -b && vite build`，类型检查不过不会进入打包
- **解决**：先跑 `npx tsc -b` 把类型错误清干净再 build（P002 即由此暴露）
- **教训 / 原则**：build = 类型检查 + 打包两段；报错位置在 `tsc` 段时不要去找打包问题，先看 TS 错误

---

## P004｜编辑器整段替换失败："text not found"

- **现象**：对 `App.tsx` 做大段/全文件替换时匹配不到旧文本
- **原因**：文件在两次读取之间被格式化工具改写了排版（压缩单行 → 多行、引号风格变化），手里的"旧文本"已过期
- **解决**：重新 `read` 当前真实内容，改用**小锚点分片替换**（imports / 函数签名 / 单条 JSX 属性等唯一片段），不再整文件覆盖
- **教训 / 原则**：批量编辑前先重读文件确认格式；编辑后读回验证；大改动拆成唯一锚点的小步提交，失败面小、好回滚

---

## P005｜编辑误删类型定义（手工失误）

- **现象**：给 `types.ts` 追加类型时用整行替换，误把 `ChartPointSeries` 原定义删掉，`ChartData` 连锁报类型错误
- **原因**：替换操作的新文本里漏带了原行
- **解决**：发现后立即读文件比对，恢复原定义，随后重新构建验证
- **教训 / 原则**：替换型编辑务必把"要保留的原行"写进 new_text；每次改动后都跑一次类型检查，能在 1 分钟内暴露这类误伤

---

## P006｜本地服务打不开 / 被代理影响

- **现象**：shell 配了 HTTP/SOCKS5 代理（`127.0.0.1:7898`），担心访问 `localhost:5173` 受影响
- **原因**：代理只作用于"出网流量"；本地回环服务不受代理环境变量影响。真正的风险点是 curl 会把 localhost 也送进代理，或代理软件劫持了 localhost
- **解决**：curl 用 `--noproxy '*'` 验证；浏览器打不开时改用 `http://127.0.0.1:5173/` 或在代理软件 Bypass 列表加 `localhost;127.0.0.1`
- **教训 / 原则**：本地联调 = 回环流量，与代理无关；遇到"装好却访问不到"先分清是代理劫持还是服务没起

---

## P007｜产品流程假设了数据已存在（冷启动缺口）

- **现象/洞察**：搭建文档 MVP 流程是"打开 Dora → 看到业务脉搏"，但它默认数据已在系统里；真实产品第一次打开没有任何数据
- **讨论结论**
  1. "上传要等整个系统跑通"是**伪依赖**——"数据入口（上传/解析/落库）"和"上传后自动触发分析引擎"可以解耦
  2. 首屏应为**无数据引导 + 上传/样例选择**，并把上传当作整条价值链的第 0 环（Data 是核心循环的起点）
  3. 上传不是一次性 onboarding：日常"更新数据"复用同一入口（对应文档中 09:32 数据更新场景）
- **落地**：迭代 3 前端 mock 版（引导页 → 数据就绪 → 业务脉搏；更新/清除入口），真实解析随后端接
- **教训 / 原则**：Demo 流程 ≠ 产品流程；凡"数据驱动的 AI 产品"都要先回答"数据第一次从哪里来、空状态长什么样"，它属于闭环起点而不是收尾功能；开发顺序上优先做"竖切（上传本身）"，把"完整流水线触发"交给引擎阶段

---

## P008｜vite.config 代理改动没生效，且端口被残留进程占用

- **现象**：给 `vite.config.ts` 加了 `/api` 代理后，`curl localhost:5173/api/health` 返回的是前端 HTML（SPA fallback），不是后端 JSON
- **原因**：正在运行的 dev server 未重新加载新配置（Vite 配置改动不一定热重启）；同时旧 dev 进程未被清理，新起服务自动 fallback 到 5174
- **解决**：杀掉占用 5173/5174 的所有进程 → 重新 `npm run dev` → 再次 curl 验证代理返回 `{"status":"ok"...}`
- **教训 / 原则**：改动 `vite.config.ts` 后**主动重启 dev server** 并用 curl 验证；重启前用 `lsof -ti tcp:5173 | xargs kill` 清干净端口，避免出现"明明起了新服务却在 5174"的假象

---

## P009｜占位证据错配在专项审查中才暴露（c3）

- **现象**：change 洞察 c3「新品销量 ↑35.2%」的证据链 `/api/evidence/c3` 首行是「利润率 18.4%」——`evidence_kind` 一直挂在 `margin` 占位，直到 C1+C2 全流程审查才被发现
- **原因**：迭代 6 引入新品信号时把证据临时指向 `margin` 并加注释"证据由引擎扩展阶段补全"，后续没人回头替换；且自检没有对 c3 证据做断言
- **解决**：新增 `new_sku` 证据 kind（周/销量/环比），`sig-new-sku` 指向它，engine_check 增加"kind≠margin 且首行含新品销量"断言
- **教训 / 原则**：**占位数据必须配"待办钩子 + 防错断言"**；"注释说以后补"没有任何约束力，审查/自检要能抓住证据与洞察主题不一致的错配

## P010｜边界审查发现：只清 series 会误产 c2/o2、证据时间硬编码

- **现象**：边界探测中仅清空 `metric_series`（update_log/cluster 残留）时，引擎仍产出 c2/o2（pulse 1/1）；`engine_evidence.updated` 在最新事件变化后仍显示旧日期
- **原因**：c2/o2 规则只读事件/集群表，没有"存在指标序列"的门禁；证据时间戳在引擎里硬编码字符串
- **解决**：`run_engine` 增加空数据门禁（无系列 → 0/0/0/0 + `--`，不发任何洞察）；`pulse.last_updated` 与 `engine_evidence.updated` 改从最新 `data_update` 取值（无事件 `--`）
- **教训 / 原则**：**派生洞察必须以"其依据的基础数据存在"为前提**；硬编码展示字段（时间/来源名）应来自数据而非常量

## 通用经验速查
1. 前端新工程 → 先 `npm install`；编辑器还红 → Restart TS Server。
2. Vite 工程一定保留 `src/vite-env.d.ts`；类型错误先怀疑"缺声明/缺依赖"。
3. `npm run build` 的报错段位（tsc / vite）决定排查方向。
4. 编辑文件前重读真实内容（可能被格式化改写）；用唯一锚点做小步替换；改完读回 + 构建。
5. 本地回环服务不受终端代理影响；联调问题先排除代理劫持。
6. 产品闭环从"数据入口 + 空态"开始，别让 Demo 的"数据已存在"假设掩盖冷启动需求。
7. Mock 与真实 API 边界（`doraApi.ts`）保持清晰，功能演示推进不阻塞后端演进。
8. 改 `vite.config.ts` 要手动重启 dev server 并 curl 验证；重启前清空端口，防止服务悄悄换到 5174。
9. 前后端字段契约用同一套命名（camelCase），后端种子数据与前端 `data.ts` 保持同源，替换数据源时 UI 零改动。
10. 对"单行超长组件"的改造：不要硬上全量 prop 注入；可用"数据层原位同步（同一引用 mutate + 重渲染）"过渡，视觉零风险，等后端引擎接管判断字段后再切正式契约。
11. 追加/编辑列表类文档时优先用"在结尾插入"，别用"替换上一行"的写法（P005 已因此误删一次，教训通用化）。

---

## 决策记录（D 系列）

> 目的：记录"**什么决策、为什么、什么时候做的**"以及"什么条件下可以推翻"。
> 相比 P 系列（问题与修复），D 系列记录的是**没有报错也值得记住的选择**，防止后人"重新发明"或"误判为临时 hack"。

### 一览表

| 编号 | 时间 | 决策一句话 |
|---|---|---|
| D001 | 2026-09-06 | v9 HTML Demo 迁移为 React+TS+Vite 前端，后端预留 FastAPI/PG/Redis/Agent 边界 |
| D002 | 2026-09-06 | 冷启动是产品第 0 环：首屏=数据引导（上传/样例），不是业务脉搏 |
| D003 | 2026-09-06 | 演示模式的"假上传"要诚实标注，不做自欺欺人的解析 |
| D004 | 2026-09-06 | API 字段契约采用 camelCase 与前端 TS 同源；后端种子镜像 `data.ts` |
| D005 | 2026-09-06 | API 层三模式：auto / mock / http，无后端也能完整演示 |
| D006 | 2026-09-06 | 页面路由用 URL hash（不引 react-router），支持刷新恢复上下文 |
| D007 | 2026-09-06 | 行动加入/关注状态暂存 localStorage，后端状态模型落地后再迁移 |
| D008 | 2026-09-06 | 页面切 HTTP 用"原位同步 mutate + 重渲染"，不硬拆单行超长组件 |
| D009 | 2026-09-06 | V2 引擎与业务对象稳定前，不引入 Agent / LangChain / Redis 调度 |
| D010 | 2026-09-06 | 后端先 Mock Repository 通 API 契约，再上 PostgreSQL |
| D011 | 2026-09-06 | 演示语义字段（Pulse 数字/原因/下一步/图表）暂留前端，等 V2 引擎接管 |
| D012 | 2026-09-06 | tsc -b 构建产物重定向 `node_modules/.tmp`，杜绝过期 `vite.config.js` 与脏提交 |
| D013 | 2026-09-06 | Git 首次基线 + 本地身份，版本可回滚；后续每功能点一提交 |
| D014 | 2026-09-06 | V2 引擎先于 PostgreSQL/LLM：纯 Python 确定性引擎先行，文案模板占位 Reasoning |
| D015 | 2026-09-06 | 判断语义（semantics）内联进 `/api/insights` 下发，而非独立 detail 轮询 |
| D016 | 2026-09-07 | 引擎数据源落 PostgreSQL：SQLAlchemy Repository + Docker PG16 + 幂等 seed + 规则参数配置化 |
| D017 | 2026-09-07 | C3 上传取舍：规范引擎格式优先、CSV 标准库+openpyxl（不引 pandas）、事务替换+样例一键还原、证据 DB 化后置 |
| D018 | 2026-09-07 | C4 映射取舍：单值列映射契约 + 预览先行 + 不做自动推断（组合口径不支持） |
| D019 | 2026-09-07 | C5：跌破阈值自动升级 e2 + `tests/run_all` 一键门禁纳入提交前 gate（API 级 E2E，不加 pytest/httpx 依赖） |
| D020 | 2026-09-07 | 收尾清理：死代码删除、阈值单一来源（seed 由 engine.RULE_DEFAULTS 派生）、证据行 DB 化（等价重构，行为零变化） |
| D021 | 2026-09-07 | V2 边界修复（fix-v2-critical-boundaries）：空数据门禁 + 阈值语义函数化（跌破含等）+ 时间动态化 + 前端名称下沉/静态标注；后端+前端合并单 change 便于测试发布 |
| D022 | 2026-09-07 | V3 planning 决策：ReasoningProvider 抽象（模板为默认）+ 语义缓存表 + 只刷新文案不改判定（红线：LLM 不得产出数字/触发条件）+ 无 key 自动降级 template |
| D023 | 2026-09-07 | V3-T1 落地形态：推理抽象层先行（协议+模板 provider+配置+parity 测试），引擎接线推迟到 T2（语义缓存） |
| D024 | 2026-09-07 | V3-T2：insight_reasoning 语义缓存 + run_engine 合并（命中覆盖 + reasonSource/generatedAt）；semantics 收编 reasoning 包；无失效策略留 T3/T5 |

### D001｜技术栈与工程形态选型

- **时间**：2026-09-06（仓库基线）
- **决策**：把单文件 v9 HTML Demo 迁移为 React 19 + TypeScript + Vite 的 feature-based 前端；后端用 FastAPI 起步并保留 PostgreSQL / Redis / Agent 的工程边界。
- **为什么**：单文件 Demo 无法接真实数据、无法多人协作、无法演进出 Agent/调度；文档《完整详细的搭建说明流程.md》明确目标是"可运行、可演进、可接真实业务与 Agent 的产品"。
- **影响与代价**：前端承担了 Demo 的全部展示/交互；后端判断能力需逐步补齐。feature 目录（pulse/insight/action/watch）成为领域语言。
- **状态**：生效。

### D002｜冷启动是产品第 0 环

- **时间**：2026-09-06（迭代 3）
- **决策**：无数据时首屏是"数据引导页"（上传 / 载入样例），数据就绪后才进入业务脉搏；Topbar 提供"更新数据/清除"入口。
- **为什么**：搭建文档 MVP 流程隐含"数据已存在"，但真实产品第一次打开没有任何数据；上传是价值链 `Data→…` 的起点，不是收尾功能。
- **影响与代价**：前端增加 localStorage(`doraDataSource`) 门禁；"上传后自动触发分析"的最后一公里要等后端 Ingest 接通。
- **状态**：生效；后端真实上传解析（`POST /api/datasets`）待接入。

### D003｜演示模式的"假上传"诚实标注

- **时间**：2026-09-06（迭代 3）
- **决策**：自定义上传只取文件名并走内置样例运行完整流程，界面明确提示"真实解析随后端上线"；不假装真的解析了 xlsx。
- **为什么**：给用户/评审"假解析成功"的错觉，比少一个功能更伤信任。
- **影响与代价**：功能不完整是可见的，但换来诚实的产品边界。
- **状态**：生效。

### D004｜API 字段契约 camelCase、种子同源

- **时间**：2026-09-06（迭代 4）
- **决策**：后端 Pydantic 字段与前端 TS 类型保持**同一套 camelCase 命名**（`rawRows/caseTitle/tagCls`）；后端 `data.py` 镜像前端 `data.ts`。
- **为什么**：契约一处定义、两端共享语义，前端切换数据源时**零适配层、零 UI 改动**；避免 JSON 里再来一层 snake↔camel 映射。
- **影响与代价**：牺牲了后端惯例 snake_case；若未来对外部开放 API 再评估是否加映射层。
- **状态**：生效。

### D005｜API 三模式 auto / mock / http

- **时间**：2026-09-06（迭代 4）
- **决策**：`doraApi.ts` 支持 `VITE_API_MODE`：`auto`（HTTP 优先、失败静默回退 Mock）、`mock`（不请求）、`http`（失败抛错）。
- **为什么**：默认 auto 保证"没后端也能演示、接上后端立刻真实"；联调/排障用 http 让问题第一时间暴露，而不是被回退掩盖。
- **影响与代价**：auto 模式下生产环境若后端挂掉会静默降级，需在部署时用 `http` 或监控兜底。
- **状态**：生效。

### D006｜页面导航用 URL hash

- **时间**：2026-09-06（仓库基线）
- **决策**：不引入 react-router，用 `location.hash`（`#page=&type=&idx=`）+ `hashchange` 恢复页面上下文。
- **为什么**：Demo 页面层级浅、依赖少、刷新/分享可恢复；避免为 4 个页面引入路由全家桶。
- **影响与代价**：未来若做深层路由/懒加载需评估是否换 react-router（触发器：页面数 > 8 或需要嵌套路由/守卫）。
- **状态**：生效。

### D007｜行动/关注状态暂存 localStorage

- **时间**：2026-09-06（仓库基线）
- **决策**："已加入行动回路"清单存在 `localStorage`（`doraJoinedAction`），不依赖后端。
- **为什么**：后端状态模型尚未落地；用本地持久化先把"加入→执行→回写"的交互跑通，演示不因刷新丢失。
- **影响与代价**：这是**临时状态源**，V2 状态模型（action_case/action_step 表）落地后需迁移并做双写兼容。
- **状态**：生效（待迁移）。

### D008｜页面切 HTTP 采用"原位同步"，不硬拆组件

- **时间**：2026-09-06（迭代 5）
- **决策**：不把 Pulse/Insight/Action/Watch 改造成"prop 注入 + 加载态"，而是 App 启动同步器把后端列表**原位刷新**到 `data.ts` 同一批数组/对象再触发重渲染。
- **为什么**：这些页面是超长压缩单行组件，硬拆会带来大改动与回归风险；同步器模式让页面"无感"切换为后端数据，视觉与契约不变。
- **影响与代价**：`data.ts` 仍是前端双写源，架构上是过渡态；等 V2 引擎把判断字段上移到后端后，再按正式契约重构组件（见 D011）。
- **状态**：生效（过渡态）。

### D009｜稳定前不引入 Agent / LLM / Redis 调度

- **时间**：2026-09-06（基线约定，持续生效）
- **决策**：在业务对象、数据流、Rule/Signal/Insight 引擎稳定前，不引入 Agent / LangChain / MCP / Redis 定时调度。
- **为什么**：搭建文档第 29 章明确警告——先跑通确定性规则与业务对象，再谈智能编排，否则复杂度失控。
- **影响与代价**：功能上"AI 味"暂时不足；但换来可测试的引擎与可解释的证据链。
- **状态**：生效（触发器：Metric→Rule→Signal→Insight 链路在 PostgreSQL 上跑通后，才进入 Reasoning 阶段）。

### D010｜后端先 Mock Repository，再上 PostgreSQL

- **时间**：2026-09-06（迭代 4）
- **决策**：FastAPI 端点先返回内存 Mock Repository（`app/data.py` 镜像前端数据），不直接连 PostgreSQL。
- **为什么**：先把 API 契约与前端消费打通，DB 迁移不阻塞契约验证；符合"一次只引入一个变化"的原则。
- **影响与代价**：数据重启即丢；PostgreSQL + SQLAlchemy 层是明确的下一个后端任务。
- **状态**：生效（待替换）。

### D011｜演示语义字段暂留前端（已于迭代 8 收尾/部分反转）

- **时间**：2026-09-06（迭代 5 提出；迭代 8 收尾）
- **决策**：Pulse 摘要数字、Insight 的原因/下一步建议、图表数据等"判断语义"暂时留在页面内，不搬到后端。
- **为什么**：这些文案本质是"后端判断结果"，但 V2 Insight Engine 尚未存在；现在搬运=在后端复制死数据，无增益且与 D009 冲突。
- **影响与代价**：前端仍有"判断残留"，违反长期边界原则；D011 的**退出条件**= V2 引擎上线并扩展 `GET /api/insights/{id}` 详情契约。
- **收尾结果（迭代 8）**：V2 引擎上线后，把原因链 / 下一步建议 / Pulse 主线"判断"字段上移为引擎 `semantics`（内联进 `/api/insights`），前端只保留离线 fallback；**图表配置留在前端**，视为展示层而非判断语义。
- **状态**：主体收尾（D015 记录新契约形态）。

### D012｜构建产物重定向，杜绝过期配置与脏提交

- **时间**：2026-09-06（git 基线时发现并修复）
- **决策**：`tsconfig.app.json`/`tsconfig.node.json` 的 `tsBuildInfoFile` 与 node 的 `outDir` 全部指向 `node_modules/.tmp`；`.gitignore` 兜底忽略 `*.tsbuildinfo`、`vite.config.js/.d.ts`。
- **为什么**：`tsc -b` 曾在前端根目录生成 `vite.config.js`（过期生成物，可能覆盖手写 `.ts` 被 Vite 优先加载）与 tsbuildinfo（污染首次提交）；重定向后仓库干净、配置单一事实源。
- **影响与代价**：构建增量缓存挪到 git 忽略区；无功能影响。
- **状态**：生效。

### D013｜Git 基线：本地身份 + 每功能点一提交

- **时间**：2026-09-06
- **决策**：仓库 `git init`（main 分支），身份仅写入本仓库（`tomara <tomara@mac>`，全局不动）；约定后续每个功能点/文档变更独立提交。
- **为什么**：此前没有版本控制，无法回滚与对比；小粒度提交让"实际开发过程记录"与 commit 一一对应，便于回放每个迭代。
- **影响与代价**：本地身份不用于 GitHub 提交时需另配；推送远程前确认身份与远程仓库地址。
- **状态**：生效。

### D014｜V2 引擎先于 PostgreSQL / LLM：确定性计算先行

- **时间**：2026-09-06（迭代 6）
- **决策**：先实现纯 Python + 内存数据集的 **Metric→Rule→Signal→Insight** 确定性引擎（`backend/app/engine/`），暂时不连 PostgreSQL、不接 LLM；文案叙事用**模板占位**（标注"V3 Reasoning 替换"）。
- **为什么**：文档 D009/D010 原则——先让"判断逻辑"可测试、可解释，再谈持久化与智能编排；模板文案是"判断结果的可读层"，把判断公式与文案模板分离后，将来接 LLM 只替换文本层，不动判定逻辑。
- **影响与代价**：当前引擎结论与静态快照并存（`/api/engine/*` 独立端点），数值已计算但前端尚未消费；需要下一步把 `/api/insights*` 指向引擎，并同步把页面内置语义字段上移契约（D011 退出条件）。
- **状态**：生效（过渡态）。

### D015｜判断语义内联进洞察列表下发

- **时间**：2026-09-06（迭代 8）
- **决策**：引擎每条洞察把 `semantics`（原因链/下一步）作为字段**内联在 `/api/insights` 列表**返回，页面选择某条时直接使用，不再单独请求 detail。
- **为什么**：洞察集每次全量刷新（页面同步器模式），语义随列表一次到达，无第二跳、无 loading 态、离线 fallback 与在线数据同构；列表与详情不会出现"数值新、语义旧"的错位。
- **影响与代价**：列表 payload 变大（含每条的 cause/next/evidence）；契约需显式建模 `semantics` 字段，不能再用瘦身 response_model 过滤。
- **状态**：生效。

### D016｜引擎数据源落 PostgreSQL：Repository + 规则配置化

- **时间**：2026-09-07（迭代 10 / C2）
- **决策**：SQLAlchemy 2.0 数据层，本地用 Docker PostgreSQL 16 端到端；四张表（`metric_series` / `store_cluster_store` / `data_update_log` / `rule_config`）；`dataset.py` 降级为"种子源"；引擎改为 `compute_snapshot(repo, rules)`，阈值走 `rule_config`（DB 值优先、`RULE_DEFAULTS` 回退）。
- **为什么**：内存常量随进程丢失、规则不可调，无法接真实数据（C2 目标）；判定逻辑与数据源解耦（Repository），错误语义明确（DB 离线 → 503，绝不静默回退 Mock）。
- **影响与代价**：运行依赖数据库在线；REAL 列曾致整数计数显示 `4,286.0`（已由 engine_check 断言兜住）；`psycopg[binary]` 因 Py3.14 wheel 从 3.2.9 升 3.2.13；迁移工具（Alembic）尚未引入。
- **状态**：生效。退出条件/后续：真实上传导入（C3）将写入同一 Repository；证据行若需完全脱离 `dataset.py`，可在 C3 一并处理。

### D017｜C3 数据上传的关键取舍（即时落档于 propose）

- **时间**：2026-09-07（C3 `dataset-upload` propose）
- **决策**：
  1. 首版上传采用**"引擎输入规范格式"**（表头 `metric_key,label,dimension,value,unit`，支持 demo 8 类指标 key），不做任意业务文件的智能映射；
  2. 解析用 **CSV 标准库 + openpyxl（xlsx）**，**不引入 pandas**；
  3. 上传语义 = **事务性"删旧+插新"替换该文件涉及的指标 key**，并登记 `data_update_log`；`POST /api/datasets/sample` 一键还原演示数据；
  4. **证据行 DB 化后置**（不随 C3 做）。
- **为什么**：规范格式与 Repository 模型一一对应、无歧义，可先跑通"文件→入库→引擎反映"主链路；pandas 体积/兼容风险大于收益；"上传即换数据源 + 一键还原"语义最贴近冷启动/换数据场景；避免 C3 一次吞下 C4（口径映射）与证据重构。
- **影响与代价**：真实业务文件需先转规范格式或等 C4 映射向导；xlsx 大文件解析慢 → 设计限制 10MB/5 万行；替换会覆盖演示数据（可用样例接口还原）。
- **状态**：生效（propose）。退出条件：C4 引入通用映射后格式能力外扩；若发现真实场景需要 pandas 再重评估"不引 pandas"。
- **apply 期补充（2026-09-07）**：`POST /api/datasets/sample` 语义定为**出厂重置**——先清空 `metric_series` 与 `data_update_log` 再写入规范种子（幂等、计数恒定 58/10/1/6），避免此前上传的残留行污染演示数据；代价是样例不可"保留用户追加的其它 key"（demo 阶段可接受）。

### D018｜C4 映射契约的取舍

- **时间**：2026-09-07（C4 `upload-mapping` propose）
- **决策**：
  1. 映射契约 = **单值列**：`metric_key` + `label_column` + `value_column` + 可选 `dimension_column`/`unit`；
  2. **预览先行**（`/datasets/preview` 只解析不落库）再映射确认；
  3. **不做自动推断**（不猜哪列是数值/日期/维度）。
- **为什么**：映射契约最小、可读、前后端/API 双用；预览让"声明列"有据可依，减少误选；自动推断（ML/启发式）收益尚不明确，贸然引入会带来不可解释的映射错误。
- **影响与代价**：组合口径（如"收入-成本"或跨列归一）不支持——列为显式非目标；用户需知道 metric_key 白名单；预览整文件（xlsx read_only 流式，成本可控）。
- **状态**：生效（propose，apply 无偏离）。退出条件：出现真实"组合口径/自动推断"需求时，扩展 mapping 结构或单开能力。

### D019｜C5 自动升级与门禁的取舍

- **时间**：2026-09-07（C5 `e2e-guardrails`）
- **决策**：
  1. 华东订单跌破阈值 → 引擎自动生成 problem `e2`（不再是"待升级 change"）；
  2. 门禁 = `tests/e2e_api_check.py`（运行中 API 的 4 核心用例，脚本内自重置）+ `tests/run_all.py` 串联 engine/ingest/e2e，**纳入 change 归档/提交前 gate**；
  3. 保持零新测试依赖（`python3 -m tests.*` + 系统 curl），不引 pytest/httpx。
- **为什么**：Case 4 需要真实"升级为 Problem"行为否则无法验收；门禁把散装命令固化，避免"任务自测过≠规格全过"再次发生；零新依赖贴合既有运行前提（DB+后端在线）。
- **影响与代价**：E2E 依赖本地运行后端（文档明示前置）；e2 仅为最小升级语义，完整 Watch 委托/调度属 V4；`StringIO.read()` 与自拼 multipart 兼容坑在实现期暴露并修复（以 curl 兜底文件上传）。
- **状态**：生效（apply 完成）。退出条件：CI 环境就绪后可把 run_all 挂到 CI；若需并发/隔离再评估 pytest。

### D020｜收尾清理的取舍

- **时间**：2026-09-07（code-cleanup，审查遗留）
- **决策**：删除 `doraApi.isApiLive/liveSet` 死代码；阈值默认值收敛为**单一来源**（`seed.rule_items()` 由 `engine.RULE_DEFAULTS` 派生，DB `rule_config` 有值仍优先）；`engine.py` docstring 更新为现状；**证据行 DB 化**（`_evidence_rows` 读 Repository，删除对 `dataset.rows_for_evidence` 的运行时依赖）。
- **为什么**：等价重构优先于加新功能——死代码增加阅读噪音，双源阈值必然漂移，过期 docstring 误导，证据读常量与"引擎从持久化计算"的语义冲突。
- **影响与代价**：`dataset.py` 保留为纯种子源（供 seed/数据定义），不再被引擎运行路径消费；行为等价由 `tests/run_all` + 冒烟 + 前端 build 证明。
- **状态**：生效（apply 完成）。后续：仅剩"数据源名称下沉页面文案、引擎与页面数字一致性核对"等非代码项可另行处理。

### D021｜V2 边界修复合并为一个 change

- **时间**：2026-09-07（fix-v2-critical-boundaries）
- **决策**：把边界审查的后端修复（空数据门禁/阈值语义函数化/时间动态化）与前端修复（数据源名称下沉/静态标注）**合并为一个 change**，便于统一测试与发布（分支 `fix/v2-critical-boundaries`，OpenSpec change 名 `fix-v2-critical-boundaries`）；阈值语义固化：目标线"低于"用严格 `<`，升级"跌破"默认含等 `<=`。
- **为什么**：修复彼此关联（门禁影响证据/端点、时间影响前后端展示），单 change 可一次跑通 `tests/run_all`+build 门禁后发布，避免碎片化提交难验证。
- **影响与代价**：Watch/Action 静态域不改（V4）；`evidence.updated` 日期用当天（事件仅存 HH:MM，为显式边界）；RELEASE_NOTES_V2 用脚注标注 V2 Watch/Action 为静态演示。
- **状态**：生效（apply 完成，待归档）。退出条件：V4 上线动态委托后可移除页面 `?` 静态标注与脚注。

### D022｜V3（Dora Reasoning）规划决策

- **时间**：2026-09-07（路线图 §3A planning）
- **决策**：
  1. 解释/总结/建议的来源抽象为 `ReasoningProvider`，**引擎模板是默认 provider**（行为不变、无 LLM 全绿）；
  2. 新增 `insight_reasoning` 缓存表 + `/api/insights` 合并缓存语义（无缓存回退模板并标注 `reasonSource`）；
  3. 提供 `POST /api/reason/refresh`（幂等批量）与前端"重新解释"入口；
  4. **红线：LLM 只动文案，不产数字/触发条件/证据**；无 key/网络错误自动降级模板。
- **为什么**：V3 的差异化价值在"解释质量"而非"判断能力"（D009/D014）；把文本层与判定层彻底解耦才能保证引擎可测、解释可替换、无 LLM 也能发布。
- **影响与代价**：文案不再"每一次确定性一致"（缓存+provider 标注缓解）；依赖 OpenAI 兼容接口（可换）；新增少量表/端点。
- **状态**：规划中（进入条件已满足，等待授权开工 V3-T1）。退出条件/触发：V3-T1..T5 验收清单全过 → 版本速览标 V3 已完成。

### D023｜V3-T1 推理抽象层落地形态

- **时间**：2026-09-07（V3-T1 `v3-reasoning-layer`）
- **决策**：T1 只交付**抽象层**（协议 + TemplateProvider + `resolve_provider()` + parity 测试），**不接线引擎、不加 API**；engine 语义生成收编与缓存接线推迟到 T2。
- **为什么**：以"零行为变化 + parity 测试"证明抽象层不引入漂移，再在 T2/T3 放心替换；避免一次性大改引擎。
- **影响与代价**：TemplateProvider 延迟导入引擎私有 `_semantics`（避免启动循环），引擎私有函数成为临时接缝；`run_all` 增至 4 段（含 reasoning_check）。
- **状态**：生效（迭代 17，已归档）。退出条件：T2 语义缓存接线时把 `_semantics` 逻辑收编进 reasoning 包，消除该接缝。

### D024｜V3-T2 语义缓存与收编

- **时间**：2026-09-07（V3-T2 `v3-semantics-cache`）
- **决策**：新增 `insight_reasoning`（insight_id 唯一 + semantics JSON + provider + generated_at）；`_semantics` 迁移为 `reasoning/semantics.template_semantics`（引擎别名兼容，D023 退出）；`reasoning/cache.refresh` 幂等写缓存；`run_engine` 合并缓存——命中覆盖 semantics 并标 `reasonSource`/`generatedAt`，未命中保持模板并标 `reasonSource="template"`（不写库，避免无 key 时膨胀）。
- **为什么**：语义可被外部 Provider（未来 LLM）替换并持久化，同时对未配置/无缓存环境完全回退到 V2 行为；收编消除"reasoning 依赖引擎私有函数"的接缝。
- **影响与代价**：每条洞察多一次缓存查询（N+1，≤9 可接受）；**缓存无失效策略**（数据集变更后旧解释仍命中）→ 明确留 T3/T5 的 refresh 与指纹失效。
- **状态**：生效（迭代 18，已归档）。退出条件：T3 引入 LLM Provider + `/api/reason/refresh` 并补失效策略。
