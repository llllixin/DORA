# Dora · 版本与方向看板（单一事实源）

> 目的：一眼看清「现在是哪个版本 / 该版本还剩什么 / 下一版做什么」。
> 约定：每次迭代或 OpenSpec change 归档后，**必须**回到本文刷新「当前状态」与待办勾选。
> 参考：docs/完整详细的搭建说明流程.md（第 28–36、42 章）；docs/开发过程记录.md（迭代日志）；
> docs/开发问题与经验.md（P/D 系列）；openspec/（变更级 spec-driven 流程）。

---

## 1. 版本速览

| 版本 | 含义 | 状态 | 对应迭代 |
|---|---|---|---|
| Demo v9 | 单文件 HTML 产品基线 | 已完成（只作产品参考） | 基线 |
| **V1 · 第一个可运行版本** | 四大页面 + 证据链 + Mock/HTTP 服务层 + hash 导航 + localStorage + 冷启动 | ✅ 已完成 | 迭代 0–5 |
| **V2 · 真实业务分析** | Metric/Rule/Signal/Insight 引擎 + PostgreSQL + 上传为输入源 | ✅ **已完成**（C1–C5 全归档，迭代 6–14；`tests/run_all` 全绿） | 迭代 6–14 |
| V3 · Dora Reasoning | 接 LLM，只做解释/总结/建议（替换引擎文案模板） | ✅ **已完成**（迭代 17–21，验收通过；已知问题 P011/D028 标注跟踪） | 17–21 |
| V4 · Watch 真实委托 | 意图解析 → WatchTarget → 规则 → 定时调度 | ✅ **已完成**（迭代 22–26，验收通过；见 `docs/V4_ACCEPTANCE_CHECKLIST.md`） | 22–26 |
| V5 · Action 真闭环 | Case→Step→执行→验证→关闭，结果决定关闭 | 🔶 **已实现、待人工验收**（V5-T1..T5 已归档 迭代 28–33，见 §3C & `docs/V5_ACCEPTANCE_CHECKLIST.md`） | 28–33 |

**当前答案：V1–V4 已完成（迭代 0–27）；V5（Action 真闭环）已实现、待人工验收（迭代 28–33 全归档，run_all 7 段全绿，见 docs/V5_ACCEPTANCE_CHECKLIST.md C 段）；sign-off 后 V5 标完成，再讨论 V6（工具/Agent 执行层等）或 LLM 兜底阶段 2。**

---

## 2. V2 剩余内容（当前唯一"打开"版本）

按建议的 OpenSpec change 拆分（每项独立 propose → apply → archive）：

- [x] **C1 · 引擎覆盖补齐** ✅（迭代 9 / OpenSpec change `extend-engine-coverage`）：c2（数据更新事件）、o2（华东门店集群）已纳入规则覆盖，证据/语义/计数全部引擎化；残留 = 退货 SKU·新品门店等证据深度挖深、规则阈值配置化（并入 C2）
- [x] **C2 · PostgreSQL Repository** ✅（迭代 10 / OpenSpec change `postgres-repository`）：SQLAlchemy 四表 + Docker PG16 + 幂等 seed + `compute_snapshot(repo, rules)` + 规则阈值配置化（改 `rule_config` 即生效，缺省回退） + DB 离线 503（不回退 Mock）
- [x] **C3 · 真实数据上传** ✅（迭代 12 / OpenSpec change `dataset-upload`）：`POST /api/datasets`（csv/xlsx 规范格式→400/413/503）事务替换落库、`/datasets/sample` 出厂重置、`/datasets/current` 摘要；前端样例/上传接真实后端（离线回退）
- [x] **C4 · 判断语义再收敛** ✅（迭代 13 / OpenSpec change `upload-mapping`）：上传后"列→口径"映射（预览 + metric_key/value/label/dimension 声明入库），承接 Metric Definition
- [x] **C5 · 质量护栏** ✅（迭代 14 / OpenSpec change `e2e-guardrails`）：4 个核心验收用例固化为 `tests/e2e_api_check.py`（自重置），`tests/run_all.py` 一键门禁纳入提交前 gate；补齐"跌破阈值→自动升级 Problem(e2)"行为
- [x] 收尾杂项 · c3 证据错配已修（change `fix-new-sku-evidence`，迭代 11）
- [x] 收尾杂项 · 数据源名称下沉（Pulse 状态栏 dataLabel，迭代 16）
- [ ] 收尾杂项 · 引擎结论与页面数字的一致性核对（低优先）
- [x] 收尾杂项（审查遗留，code-cleanup 已清）✅：`doraApi.isApiLive/liveSet` 死代码已删；`RULE_DEFAULTS` 与 `seed.rule_items` 收敛为单一来源；`engine.py` docstring 已更新；证据行 DB 化（`engine_evidence` 读 Repository）

> V2 完成判据 = C1–C5 全部归档 + 4 个 E2E 用例可自动跑通。

---

## 3. V3/V4/V5 的进入条件（防止提前做）

- **V3（LLM）** 只在 PostgreSQL 上的 Metric→Rule→Signal→Insight 链路跑通后启动（D009/D014）。
- **V4（Watch 调度）** 需要业务对象稳定 + V3 可用；引入 Redis/Celery。
- **V5（Action 闭环）** 需要 action_case/action_step 状态表 + 执行回写语义成熟。

---

## 3A. V3 · Dora Reasoning —— 详细规划（Planning）

> 定位一句话：**让"判断"保持确定性（引擎不变），只把"解释文案"从模板换成 LLM 生成。**
> 原则（D009/D014 衍生）：数值/判定/触发条件永不来自 LLM；LLM 只能引用引擎给出的事实来组织语言。

### 3A.1 范围与非目标
**范围（In scope）**
- 解释/总结/建议文本的生成来源抽象为 `ReasoningProvider`（引擎模板作为默认 provider，行为不变）。
- LLM Provider（OpenAI 兼容 chat/completions）：输入=引擎上下文（trigger/factors/evidence 行/metrics/semantics 现状），输出=与现契约一致的文案（desc/question/causeA/causeB/next/reason 的**文本部分**）。
- 语义缓存：新增 `insight_reasoning` 持久化（insight_id→payload+provider+generated_at）；`/api/insights` 内联语义先取缓存，无缓存回退模板并标注 `reasonSource`。
- 手工"重新解释"入口（前端按钮 + `POST /api/reason/refresh`）。
- 质量与降级：无 key/网络失败 → 自动回退模板（页面可见 `AI 解释失败，已回退模板`），绝不阻塞引擎输出。

**非目标（Out of scope）**
- Agent 编排 / 工具调用 / 多步推理（不进 V3）。
- Dora Chat SSE 对话流（另立能力）。
- 自动建档 action case / Watch 动态委托（V4）。
- 修改任何判定逻辑、规则、evidence 原始行。

### 3A.2 开发路线与版本任务（每项=一个 OpenSpec change，按序开工）
| 任务 | 内容 | 输出/验证 |
|---|---|---|
| **V3-T1 推理抽象层** ✅（迭代 17，已归档） | `app/reasoning/provider.py`（协议+`TemplateProvider`+`resolve_provider`）+ 配置 `DORA_REASONING_PROVIDER=template\|llm` | run_all（4 段）ALL GREEN、parity 9 洞察、build 绿 |
| **V3-T2 语义缓存** ✅（迭代 18，已归档） | `insight_reasoning` 表 + Repository + `reasoning/cache.refresh` + run_engine 合并（命中覆盖 + reasonSource/generatedAt，未命中 template 不写库）；semantics 收编 reasoning 包 | reasoning_check（parity+T2 断言）、run_all（4 段）ALL GREEN、端点标注验证 |
| **V3-T3 LLM Provider + 刷新 API** ✅（迭代 19，已归档） | `reasoning/llm.py`（OpenAI 兼容、数值稳定合并、无 key fallback）+ `resolve_provider(llm)` + `POST /api/reason/refresh` + 数据写入/出厂重置清缓存 | reasoning_check（无 key fallback/mock 稳定）、run_all ALL GREEN、端点 rows 9→0 验证 |
| **V3-T4 前端"AI 解释"入口** ✅（迭代 20，已归档） | Insight 详情来源徽标（AI/模板）+ "↻ 重新解释"（refreshReasoningApi→App 重同步）；types 加 reasonSource/generatedAt | `npm run build`、/api p1 reasonSource+semantics、冒烟全绿 |
| **V3-T5 验收与收尾** ✅（迭代 21，已归档） | golden 基线+`golden_check`（`--write` 重生成）纳入 run_all（5 段）；`V3_ACCEPTANCE_CHECKLIST.md`（自动命令+人工核对+sign-off） | run_all 5 段 ALL GREEN、golden 9 insights matched |

### 3A.3 接口信息（V3 新增，均向后兼容）
```text
POST /api/reason/refresh
  body:  { }                       # 空 = 对当前引擎全部洞察逐条生成
  resp:  { ok:true, updated:[ids], fallback:[ids], provider:"llm|template" }
  # fallback = 未配置 key 或 provider 出错被模板接管的洞察；重复调用幂等（有缓存直接返回）。

（展示层仍走既有 GET /api/insights / /api/insights/{id}）
  # 语义字段不变：semantics{causeA,causeB,next,desc…}
  # 新增只读标注：reasonSource:"llm"|"template", generatedAt?:ISO
```
```text
环境变量（backend/.env）：
  DORA_REASONING_PROVIDER=template|llm   # 默认 template（无 LLM 也可全绿）
  DORA_LLM_BASE_URL=https://api.openai.com/v1
  DORA_LLM_API_KEY=…                      # 未设置 → 自动 template 降级
  DORA_LLM_MODEL=gpt-4o-mini
```
错误语义：LLM 侧 401/429/5xx → 该洞察回退模板（返回含在 `fallback`），API 本身返回 200；引擎端点不受影响。

### 3A.4 最终验收与审查计划
1. **功能验收**：打开任意洞察详情 → 可点"AI 解释/重新解释"；来源徽标正确；无 key/断网时自动回退模板并提示；刷新后缓存复用。
2. **判定不被污染（关键红线）**：provider mock 输入同一 context 输出不同文案时，断言 metric/delta/trigger/evidence/置信度**逐字段不变**（LLM 只动文本）。
3. **金样本/回归**：`DORA_REASONING_PROVIDER=template` 时 `tests/run_all` 全绿且 `/api/insights` 与 V2 结果一致。
4. **接口审查**：新增端点契约写入 openspec（V3 各 change 的 spec delta）；README 需求来源表同步。
5. **审查人核对项**：本规划 §3A、各 change 的 proposal/spec/design/tasks、`docs/开发问题与经验.md` D022+；大版本切换：验收通过后把版本速览 V3 标为已完成（记录迭代）。

### 3A.5 进入 V4 的判据
V3 验收清单全过 + 业务对象稳定 + Watch/Action 静态域说明仍有效时，再开 V4 planning（不提前）。

---

## 3B. V4 · Watch 真实委托 —— 详细规划（Planning）

> 定位一句话：**把"持续关注"从静态列表升级为真实业务委托**：用户一句话托管目标 → 解析为 WatchTarget → 由引擎按频率评估 → 命中变化送回业务脉搏，跌破升级条件给出升级事件。
> 对应搭建文档第 12/33 章与 Case 3/4；V4 只做"委托 → 评估 → 事件 → 回脉搏"，**Action 闭环留给 V5**。

### 3B.1 范围与非目标
**In scope**
- 持久化：`watch_target`（原文 + 解析后的 metric_key/dimension/条件/频率/状态）+ `watch_event`（命中历史：时点/前后值/事件语义/是否升级）。
- 委托解析：中文语句 → WatchTarget 结构化字段；**规则词典 + 显式确认**（引擎支持的 metric key + 中文别名 + 区域名 + 条件句式）；无法映射 → 明确拒绝"暂不支持该指标"，不静默假设。
- 通用 Watch 评估器：从 `engine.py` 抽取可复用原语（`_streak_below` / `_breach` / `_below`），使"连续 N 天下跌 / 跌破 X% / 高于 X"可对任意受支持的 metric_key × dimension 求值（现 east 升级链路是特例，泛化为通用 watch 规则骨架）。
- 触发时机：① "数据更新时"——上传/样例成功后**即时**评估（与 `/datasets` 事务同链）；② "每日/每周 xx:xx"——进程内轻量调度线程 tick（**不引 Redis/Celery**，D009 维持）。
- 命中与升级：命中 → 写 `watch_event` + 回业务脉搏（Pulse「持续关注」真实化：最新事件/状态 Tag/升级提示）。升级**只以事件与标签呈现**；不伪造引擎未判定的 problem 身份。
- API：`/api/watch` CRUD + `/api/watch/parse` + `/api/watch/{id}/check`。
- Watch 页真实化：委托输入回显真实解析结果、目标列表/状态/频率来自后端、支持暂停/删除/手动检查。

**Out of scope**
- Action 闭环（Case/Step/Execute/Result/Verify/Resolve）→ V5。
- LLM 意图解析：判定不进 LLM（红线延续）；未来 LLM 只可给"候选"，必须规则收敛 + 用户确认。
- Redis/Celery 分布式调度、SSE 实时推送、Dora Chat 追问（仍另立，见 §3A 非目标）。
- 不受引擎支持的指标/映射不明确的"假装关注"（宁可拒绝，不造假）。

### 3B.2 开发路线与任务（每项 = 一个 OpenSpec change，按序开工）
| 任务 | 内容 | 输出/验证 |
|---|---|---|
| **V4-T1 Watch 领域与持久化** ✅（迭代 22，已归档） | `watch_target`/`watch_event` 表 + Repository CRUD（create/list/get/status/events/delete，会话级 DB 不可用 → 503）+ schemas | run_all 保持绿；新 repo 单测；空表 CRUD 冒烟 |
| **V4-T2 委托解析与确认** ✅（迭代 23，已归档） | `app/watch/parser.py`（指标/区域/条件/频率词典 + 句式）；`POST /api/watch/parse` 返回解析候选或 unsupported；前端建议 chips 真实映射 | parse 用例（支持句式、拒绝句式）；curl 冒烟 |
| **V4-T3 通用评估器 + 触发时机** ✅（迭代 24，已归档） | 从 engine 抽公共原语；`app/watch/evaluator.py`（evaluate_target → watch_event）；上传/样例成功后即时触发 + 进程内周期 tick（每日/每周）；事件回 Pulse watching | watch_check 核心断言（命中/未命中/升级）；`east` 现有升级链路回归不变 |
| **V4-T4 Watch/Pulse 前端真实化** ✅（迭代 25，已归档） | WatchPage 输入→parse 回显→创建；目标列表/状态来自 `/api/watch`；暂停/删除/手动 check；Pulse「持续关注」显示最近事件与 Tag | `npm run build`；curl + 冒烟；Watch 页走真实 API（后端离线才回 data.ts 兜底） |
| **V4-T5 升级路径 E2E + spec + 收尾** ✅（迭代 26，已归档） | 搭建文档 Case 3/4 固化为 watch_check（变化→watch；继续恶化→升级事件回脉搏）；首个归档 change 新建能力 spec `business-watch` + README 导航；验收清单 | run_all **6 段** ALL GREEN（新增 watch_check）；golden template 9 insights 不变；sign-off |

### 3B.3 接口信息（V4 新增；GET /api/pulse 扩展而非破坏）
```text
POST /api/watch/parse      { text } → { ok, intent?:{metric_key,dimension,condition:{type,days,ref},frequency}, unsupported?:[...] }
POST /api/watch            { text, frequency } → { ok, target:{id, parsed, status:"watching"} }   # 创建即首次评估（幂等：同 text 去重）
GET  /api/watch            → { ok, targets:[{id, name, status, frequency, logic, value, last_event_at?, escalated?}] }
GET  /api/watch/{id}       → target + events[]（时点/前后值/语义/是否升级）
PATCH /api/watch/{id}      { status:"paused"|"watching" }
DELETE /api/watch/{id}     # 同时清其事件
POST /api/watch/{id}/check → 手动立即评估（验收/测试用）
# GET /api/pulse 持续关注区：从 watch_targets 实时 + watch_events 最近命中聚合
```
频率枚举：`on_update`（数据更新时，默认）/ `daily 09:00` / `weekly`。升级语义：复用引擎阈值风格（如 `_breach`）；**升级事件 ≠ problem 身份**，是否成为正式问题仍由引擎在数据满足既有 rule 时判定（V5 把 case 接上）。

### 3B.4 红线与验收计划
1. **判定不造假**：Watch 的所有 value/前后值/命中结论来自 Repository 序列与引擎原语；watch 命中只产生 watch_event + Pulse 呈现，不伪造引擎未判定的 problem/opportunity（参考 V3 数值锁 D 系列）。
2. **支持范围显式**：只有引擎 `METRIC_SERIES_KEYS` + 已知区域/条件可建 watch；unsupported 一律拒绝并给候选，绝不静默兜底成"关注任何变化"。
3. **降级诚实**：DB down → /api/watch 系列 503（与上传一致，不回退 Mock 数据）；前端离线才走 data.ts 兜底并标注。
4. **回归锚点**：golden 9 insights 模板基线不变；e2e Case 1/2 不受影响；Case 3/4 由新 watch_check 覆盖。
5. **spec/导航**：V4 归档新建/更新能力 spec `business-watch`；README 需求来源表 + 索引同步（沿用 V3-T5 做法）。
6. **审查人核对**：本规划 §3B、各 change proposal/spec/design/tasks、P/D 记录 D029 起；验收通过后版本速览 V4 标完成。

### 3B.5 进入 V5 的判据
V4 验收清单全过（含 watch 升级路径回脉搏稳定）+ run_all 6 段全绿 + Action 执行语义需求在真实 case 数据上已澄清；再开 V5 planning（不提前）。

---

## 3C. V5 · Action 真闭环 —— 详细规划（Planning）

> 定位一句话：**把"加入行动回路"从静态页面变成真正的 Case 状态机**：洞察 → 建档 → 步骤执行 → 验证 → 解决/继续 → 归档，结果与档案回写。
> 对应搭建文档第 34 章与 Case 1/2/4 的 Action 段；V5 做"记录驱动的执行闭环语义 + API + 界面真实化"，**真实外部动作（外呼/建单/改价/写外部系统）留给工具执行层**（不提前引入）。

### 3C.1 范围与非目标
**In scope**
- 持久化：`action_case`（insight 来源/类型/编号/标题/阶段状态机 open→running→waiting_verify→resolved(归档)/continue、时间戳、档案摘要）与 `action_step`（case 归属/序号/标题/描述/证据/状态 pending→in_progress→done|blocked、note、result、完成时间）。
- **静态迁移为 seed**：现有 5 例 ACTIONS（p1/p2/p3/o1/o2）从 `data.py` 服务端静态吐页迁到 action_case/action_step 种子（保留 PRF-0831 等档案编号，ActionPage 视觉一致）；`data.ts` 仅作前端离线兜底。
- 建档引擎：problem/opportunity 洞察 → case（规则模板步骤 + 引用证据/洞察事实；**按 insight_id 幂等**——一个问题一条档案，重复建档返回既有）。引擎判定出的 problem（含 V4 Watch 升级触发、e2 跌破升级）允许建档；watch escalate 事件本身不算。
- 执行推进：步骤 start→done(带 note/result) / blocked(暂停需人工)；case 验证 outcome=resolved（归档，摘要回写）| continue（回到下一"当前行动"，保持观察）。
- API：case CRUD + 执行/验证/回写 + 统计；Action 页真实化；洞察"加入行动回路"入口打通（列表同步仍走 data.ts 原位刷新模式）。
- 回写语义：resolved 只更新档案/状态与步骤结果，**不改引擎判定与洞察语义**；Action/Pulse 计数联动真实。

**Out of scope**
- 真实外部动作执行器（工具/Agent 层；结果由人/工具回填 note）→ 工具化（V6 或另立）。
- LLM 判定执行结果/自动 resolved（确定性语义；LLM 只可辅助起草步骤文案候选，仍规则收敛 + 用户确认）。
- Watch 命中自动建档（仅引擎判定 problem 后才允许）；SSE 实时；定时重试执行。

### 3C.2 开发路线与任务（每项 = 一个 OpenSpec change，按序开工）
| 任务 | 内容 | 输出/验证 |
|---|---|---|
| **V5-T1 Action 领域与持久化** ✅（迭代 28，已归档） | `action_case`/`action_step` 表 + Repository CRUD（create/list/get/状态迁移/事件/delete，DB 不可用 → 503）+ schemas + **5 例静态 ACTIONS 迁移 seed** | 迁移后 seed 行数（5 case × ~5 step）稳定；空表 CRUD；run_all 保持绿 |
| **V5-T2 Case 建档引擎** ✅（迭代 29，已归档） | `app/action/builder.py`：insight(problem/opportunity) → case+steps（模板 + 引擎事实/evidence 引用；按 insight_id 幂等）；只允许引擎判定的 problem/opportunity 建档 | 建档用例（p1/o1/引擎 e2 升级后可建、watch escalate 不可建、重复幂等） |
| **V5-T3 执行/验证状态机 API** ✅（迭代 30，已归档） | REST：POST cases{insightId}／GET list/detail(含 steps)／steps start-done-blocked(note/result)／POST {id}/verify{resolved|continue,note}／统计；迁移 5 例以 DB 为准返回 | 状态机非法迁移拒绝、resolved 后不可继续、continue 回新当前步；curl 冒烟；e2e Case1/2 execute 契约不变 |
| **V5-T4 Action/Pulse 前端真实化** ✅（迭代 31，已归档） | ActionPage 消费 `/api/action/cases`（列表/步骤时间线/执行/验证按钮真实调用、档案回写）；洞察页"加入行动回路"入口（problem/opportunity 可见）；Pulse「需要处理/增长机会」联动 | `npm run build`；页面冒烟（后端在线=DB 档案、离线=data.ts 兜底） |
| **V5-T5 闭环 E2E + spec + 收尾** ✅（迭代 33，已归档） | 文档 Case 1/2/4 Action 段固化为 action_check（洞察→建档→步骤 done→verify resolved/continue→归档）；首个归档 change 新建能力 spec `business-action` + README；验收清单（A/B/C + Sign-off） | run_all **7 段** ALL GREEN（新增 action_check）；golden 9 不变；sign-off 后 V5 标完成 |

### 3C.3 接口信息（V5 新增；/actions 旧静态端点由真实 case 取代，形状兼容前端兜底）
```text
POST /api/action/cases            { insightId } → { ok, case:{id, code, status, steps[], ...} }   # 幂等：已有档案返回既有
GET  /api/action/cases            → { ok, cases:[{id, code, caseTitle, status, kind, tagCls, insightId}] }   # Pulse 计数源
GET  /api/action/cases/{id}       → case + steps（含各步 note/result/status 时间线）              # ActionPage 详情
POST /api/action/cases/{id}/steps/{seq}/start    → step in_progress（非法迁移 409）
POST /api/action/cases/{id}/steps/{seq}/done     { note?, result? } → step done（推进 case→waiting_verify）
POST /api/action/cases/{id}/steps/{seq}/blocked  { note } → step blocked（case 回 open）
POST /api/action/cases/{id}/verify { outcome:"resolved"|"continue", note } → resolved=归档 / continue=下一条当前行动
# 判定/洞察端点不变；档案回写不改 reasonSource/判定字段
```
Case 状态机：`open → running（任一步 in_progress）→ waiting_verify（末步 done）→ resolved（归档）`；verify continue → 回到 running（步骤推进由用户/工具回填，档案件步序不自动跳）。

### 3C.4 红线与验收计划
1. **判定不造假 / 回写诚实**：resolved 只由 `verify outcome=resolved + note` 达成；引擎指标恢复仅作档案证据不做自动 resolved；case/step 回写不改引擎判定与 reasonSource（V3/V4 红线延续）。
2. **状态机与幂等**：非法迁移（resolved 后继续、blocked 步再 done 等）返回 4xx；同 insight 只一条档案；重复 execute 幂等。
3. **迁移一致性**：5 例 seed 保留原档案编号与字段 → ActionPage 视觉与旧静态一致（仅来源变 DB）；后端离线才走 data.ts 兜底。
4. **回归锚点**：run_all（**7 段**：+action_check）；golden 9 不变；e2e Case1/2 execute 契约不变。
5. **spec/导航**：V5 首个归档新建能力 spec `business-action` + README 来源表/索引（沿用 V4 做法）。
6. **审查核对**：§3C + 各 change proposal/spec/design/tasks + P/D 记录 D031 起；验收通过后版本速览 V5 标完成。

### 3C.5 进入 V6/收尾的判据
V5 验收清单全过（Case 1/2/4 Action 段闭环在界面可用）+ run_all 7 段全绿 + 真实档案回写语义稳定；再讨论"工具/Agent 执行层"或"多数据源/调度强化"等 V6 候选（不提前）。

---

## 4. 质量锚点（每个版本验收共用）

- 文档第 42 章检查清单：build 成功、FastAPI 启动、Pulse/Insight/Evidence/Action/Watch 全链路正常、无"页面写死业务判断"、API 边界清晰。
- 4 个核心 E2E 用例（第 36 章）可运行。
- 每个 OpenSpec change 归档 = 该范围内容已验收并有文档记录。

---

## 5. 更新约定（写代码的人必须遵守）

1. 完成任一迭代或 change → 刷新本文「版本速览 / 当前状态」。
2. 新增/关闭 OpenSpec change → 在 C1–C5 清单同步勾选并写 change 名。
3. 大版本切换（V2→V3 等）→ 在版本速览移动状态标记，并说明进入条件已满足。
