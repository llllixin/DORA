# Dora · 版本路线图（当前状态入口 · 单一事实源）

> 目的：一眼看清「当前是哪个版本 / 版本历史 / 持续优化还差什么」。
> 2026-09-08 拆分：**已完成版本内容**归档到 `docs/历史版本路线.md`；**未解决/候选/backlog** 全部整理进 `docs/持续优化路线.md`。本文只保留当前状态、入口导航与质量锚点。

## 1. 单一事实源（去哪查什么）

| 问什么 | 去哪个文件 | 说明 |
|---|---|---|
| 当前状态 / 现在是第几版 | 本文 §2 | 版本入口 |
| 已完成版本的历史路线（V1–V5） | `docs/历史版本路线.md` | 只作归档，不再改动状态 |
| 未解决 / 候选 / 已选 backlog | `docs/持续优化路线.md` | **唯一 backlog**；每行 = 一个 OpenSpec change id |
| 单个变更怎么做 / 怎么验收 | `openspec/changes/<id>/`（proposal/spec/design/tasks） | 变更级事实源 |
| 发生过什么（迭代流水） | `docs/开发过程记录.md` | 只追加「迭代 N」，不含待办 |
| 反思 / 决策 | `docs/开发问题与经验.md`（P / D 系列） | 只进 P/D |
| 能力最终态 spec | `openspec/specs/`（7 个能力，新增 business-agent）+ `openspec/README.md` 索引 | |

## 2. 当前状态

- **V1–V5 全部完成并 sign-off**；V5 后收尾迭代 34–38、**3D.1-W1（迭代 39）**、**Phase0 `knowledge-lifecycle`（迭代 40）**、**3D.1-W2 `frontend-loop-insight`（迭代 41）**、**Agent 能力 `agent-tool-layer`（迭代 42）**、**`insight-panel-polish`（迭代 43，洞察展示收敛：判断列去复述 + AI 判断接入 + 抽屉/图表/脉搏页收口）**、**`action-loop-timeline`（迭代 44，行动回路页收口：档案列表对齐洞察列表 + 状态二值 pill + 步骤时间线 [控件右置] + 每步负责专家落库 + 每步数据定位 + 经验收敛到知识库）**、**`watch-delegate-flow`（迭代 45，持续关注页收口：委托卡片增量回显生命周期字段 + 「Dora 接到委托后会做什么」改为真实状态驱动流程 + 离线不伪造 + 操作列固定 208px 三按钮同排）**、**`insight-judgment-structure`（迭代 46，洞察判断三段式：引擎 `severity` 按快照显式判级 + `consequence` 确定性外推 + chat `consequence` 意图 + 前端判断列三段式 [去复述 / 两栏等高 / 离线仍完整] + 离线镜像同口径）** 均已归档（`run_all` 8 段 ALL GREEN、golden 9 不变、`npm run build` 绿）。版本总数=5，**不预命名 V6**。
- **当前打开的工作**：`action-step-expert-assign`（行动回路每步专家语义修正：引擎自动完成的步骤标「系统自动执行」、人行步骤预填系统建议并带 `system|user` 来源标记，**方向已定 A 双轨**，待 propose，见 `docs/持续优化路线.md` §2）；`expert-registry-domain`（E1 专家注册表）与 §3 W3；agent 栈剩余（向量 RAG / job·SSE 队列 / Confidence Aggregator / 真实工具）见 `docs/workflow.md` §6.2 与 §11.6。`insight-judgment-structure` 已于 **迭代 46 归档**（判断列三段式：引擎 `severity`/`consequence` + chat 后果意图 + 前端三段式，见 `openspec/changes/archive/2026-09-11-insight-judgment-structure` 与 D040）。
- 版本历史细节 → `docs/历史版本路线.md`（§1–§5）；全部 backlog/候选/审查遗留 → `docs/持续优化路线.md`（唯一）；文档职责分层 → `docs/文档地图.md`。

## 3. 质量锚点（未来任何版本 / backlog 验收共用）

- 文档第 42 章检查清单：build 成功、FastAPI 启动、Pulse/Insight/Evidence/Action/Watch 全链路正常、无「页面写死业务判断」、API 边界清晰。
- `run_all` ≥8 段 ALL GREEN、golden 9 不变、`npm run build` 绿；每块 backlog 自身红线 / 回归锚点见 `持续优化路线.md` 对应节。
- 每个 OpenSpec change 归档 = 该范围内容已验收并有文档记录（含【测试证据】+【反思】）。

## 4. 更新约定（写代码的人必须遵守）

1. 完成任一迭代 / change → 在 `docs/持续优化路线.md` 勾选对应 backlog 行（附可复现验证），并在 `docs/开发过程记录.md` 追加「迭代 N」。
2. 新增候选 / 关闭项 → 只改 `docs/持续优化路线.md`；不在其它文件复制清单。
3. 升级为正式版本（V6 或其他）→ 先在 `docs/持续优化路线.md` 加版本规划块（范围/任务/接口/验收/进入条件），命名在规划时定，本文 §2 随之刷新。
4. 反思 / 经验 → 只进 `docs/开发问题与经验.md`（P/D 系列）；归档历史不动。
5. V1–V5 的历史规划细节（§3A/3B/3C 等）以 `docs/历史版本路线.md` 为准，不再在本文维护。
6. 文档治理：状态只在 live 文件（L1）维护；快照（RELEASE_NOTES/验收清单/审计）冻结、加「截至」标注；改文档结构/归档前跑 `cd backend && python3 -m tests.check_docs`。职责分层见 `docs/文档地图.md`。

---

> 入口：历史 → `docs/历史版本路线.md` ｜ Backlog → `docs/持续优化路线.md` ｜ 参考：docs/完整详细的搭建说明流程.md、docs/开发过程记录.md、openspec/README.md、AGENT.md