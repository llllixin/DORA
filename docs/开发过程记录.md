# Dora · 实际开发过程记录

> 用途：按时间记录每一次"实际动手开发"——加了什么功能、改了哪些文件、怎么验证。
> 对应文档：《开发问题与经验.md》记录问题与解法。
> 约定：新章节**追加在本文末尾**（时间正序）；每次改完代码后回来补一条。

---

## 基线 0：仓库初始状态（代码生成，非手工改动）

- **时间**：2026-09-06（仓库生成时）
- **内容**：由《完整详细的搭建说明流程.md》一次性生成整仓骨架
  - `frontend/`：React 19 + TypeScript + Vite，feature-based 结构（pulse / insight / action / watch），`data.ts` Mock 数据，`doraApi.ts` 隔离 API 替换点，hash 导航，localStorage 行动状态
  - `backend/`：FastAPI Starter（health / pulse 最小接口）
  - `docs/`：架构说明、v9 源 HTML、搭建说明流程
- **验证**：未在本机安装依赖/构建（当时无 `node_modules`、无 `dist`）

---

## 迭代 1：环境修复 —— 让 .tsx 不再满屏报错

- **时间**：2026-09-06
- **目标**：打开 `.tsx` 后大量红波浪线，先复现、再消除
- **做了什么**
  1. 确认 `node_modules` 缺失（0 个包）→ 依赖从未安装
  2. `npm install --no-audit --no-fund`（新增 69 个包）
  3. `npx tsc -b` 类型检查：只剩 1 个真实错误
     `doraApi.ts: Property 'env' does not exist on type 'ImportMeta'`
  4. 补上 Vite 脚手架标配的类型声明文件后，`npm run build` 通过
- **改动文件**
  | 文件 | 状态 | 说明 |
  |---|---|---|
  | `frontend/src/vite-env.d.ts` | 新增 | `/// <reference types="vite/client" />`，声明 `import.meta.env` 等 Vite 类型 |
- **验证**：`npm run build` ✅（类型检查 + 打包通过）
- **备注**：esbuild 安装脚本被 npm 安全策略拦住的告警可忽略（二进制走 optionalDependencies 正常机制）

---

## 迭代 2：启动开发服务器（环境说明，无代码改动）

- **时间**：2026-09-06
- **做了什么**：后台 `npm run dev` 起在 `http://localhost:5173/`
- **验证**：curl 首页 / `main.tsx` / `App.tsx` 均 200
- **经验**：本机 shell 有代理环境变量（HTTP/SOCKS5 → `127.0.0.1:7898`），但**不影响本地回环服务**；curl 验证时需 `--noproxy '*'`，浏览器打不开时检查代理软件的 localhost 绕过列表

---

## 迭代 3：冷启动流程 —— 补上"数据从哪来"的第 0 环

- **时间**：2026-09-06
- **目标**：产品讨论结论落地——文档 MVP 流程"打开即业务脉搏"默认数据已存在；真实系统首屏应是无数据引导（上传/样例数据）
- **产品逻辑**（本次新增功能的行为）
  ```
  首次打开(无数据) → 引导页 → 上传文件 或 载入内置样例
      → 解析动画(读取→口径→检测) → 数据就绪(localStorage)
      → 业务脉搏
  Topbar「⟳ 更新数据」→ 弹层：重传 / 换样例 / 清除数据回引导
  ```
- **改动文件**
  | 文件 | 状态 | 说明 |
  |---|---|---|
  | `frontend/src/features/onboarding/OnboardingPage.tsx` | 新增 | 首次引导页：LOGO + 三步说明 + 演示模式提示 |
  | `frontend/src/components/dora/DataSourcePicker.tsx` | 新增 | 复用核心：拖拽/点击上传 + 载入样例 + 三段解析动画 |
  | `frontend/src/components/dora/DataSourceModal.tsx` | 新增 | "更新数据"覆盖层：当前数据 chip + 重传/换样例/清除 |
  | `frontend/src/App.tsx` | 修改 | 数据源门禁 + `localStorage('doraDataSource')` 持久化 + `handleReady/Apply/Clear` + 挂载弹层 |
  | `frontend/src/types.ts` | 修改 | 新增 `DataSource` 接口（name/kind/at/rows/fields） |
  | `frontend/src/components/layout/Topbar.tsx` | 修改 | 增加 `onUpdateData` prop 与「⟳ 更新数据」按钮 |
  | `frontend/src/styles.css` | 修改 | 追加 `.boot*` / `.up-*` / `.dsm-*` 样式（含移动端适配） |
- **行为细节**
  - 无数据时整个 App 只渲染 OnboardingPage（侧边栏/页面不出现）
  - 解析用三段模拟（读取与清洗 → 建立经营口径 → 检测趋势/阈值/结构），完成后写 localStorage 并跳业务脉搏 + toast
  - 弹层/页面卸载时清理解析定时器，避免"已关闭还回调"
  - 自定义上传只取文件名，界面诚实标注"以内置样例运行，真实解析随后端上线"
- **验证**：`npm run build` ✅（46 模块，较迭代前 +3）；dev HMR 生效；各新模块 HTTP 200

---

---

## 迭代 4：FastAPI REST 替换 Mock —— 前后端真正打通

- **时间**：2026-09-06
- **目标**：把 `doraApi.ts` 的本地假数据换成真实 HTTP 请求，同时保留 Mock 回退，让"换数据源但行为零变化"
- **后端（分层落地文档 Phase 2 / 开发顺序第 05–06 步）**
  - 新增 `backend/app/schemas.py`：Pydantic 模型（Insight / Pulse / Evidence / ActionCase / WatchItem），字段命名与前端 TS 保持 **camelCase 同源**（`rawRows`、`caseTitle`、`tagCls`…）
  - 新增 `backend/app/data.py`：内存 Mock Repository，镜像前端 `data.ts`（Pulse / 9 条洞察 / 9 条证据 / 5 个行动 case / 6 条关注）
  - 新增 `backend/app/routers.py`：APIRouter 提供 8 个端点
  - 修改 `backend/app/main.py`：挂载 router（原 health/pulse 迁入 router）
- **前端**
  - 修改 `frontend/vite.config.ts`：dev server 增加 `/api` → `http://localhost:8000` 代理
  - 重写 `frontend/src/services/doraApi.ts`：`BASE` 默认 `/api`（走代理）；三模式 `auto / mock / http`（`VITE_API_MODE` 控制，默认 auto = HTTP 优先、失败回退 Mock 并 console.warn）
  - 修改 `frontend/.env.example`：补充 `VITE_API_BASE_URL` 与 `VITE_API_MODE` 说明
- **API 一览**
  | 端点 | 说明 |
  |---|---|
  | `GET /api/health` | 存活检查 |
  | `GET /api/pulse` | 业务脉搏摘要（contract，页面深度接入留给后续迭代） |
  | `GET /api/insights?type=` | 三类洞察列表（problem/opportunity/change） |
  | `GET /api/insights/{id}` | 洞察详情（附 route / evidenceId） |
  | `GET /api/evidence/{id}` | 证据链（前端证据抽屉即走此接口） |
  | `GET /api/actions` `/api/actions/{id}` | 行动 case |
  | `POST /api/actions/{id}/execute` | 执行 action |
  | `GET /api/watch` | 关注目标 |
- **验证**：后端 8 端点 curl 全通（含 404）；`5173/api/*` 代理真实命中后端；`npm run build` ✅（46 模块）；证据抽屉为当前首个真正走 HTTP 的 UI 能力

---

---

## 迭代 5：页面级数据切 HTTP —— 启动时从 FastAPI 同步领域数据

- **时间**：2026-09-06
- **目标**：四大页面的"领域数据"真正来自后端，而不是继续读死 `data.ts`
- **做法（原位同步，页面零改动）**
  - 页面组件（Pulse/Insight/Action/Watch）是超长压缩单行实现，直接改成"prop 注入 + 加载态"风险高；因此采用**同步器模式**：App 进入主界面后调用 `syncRemoteData()`，探测 `/api/health`，成功则并行拉取 `/api/insights?type=…` ×3、`/api/actions`、`/api/watch`，把结果**原位刷新**到 `data.ts` 导出的同一批数组/对象（`insights.problem` 等用 splice、`actionCases` 用重建 key），再触发一次 React 重渲染 → 页面即显示后端数据，视觉与契约完全一致
  - 后端不可用时**不刷新**（本地 Mock 兜底），前端"无后端也能演示"的承诺继续成立
- **改动文件**
  | 文件 | 说明 |
  |---|---|
  | `frontend/src/services/doraApi.ts` | 新增 `detectApi()` / `isApiLive()` / `listActions()` / `listWatch()` / `syncRemoteData()` |
  | `frontend/src/App.tsx` | 新增 `setSyncTick` 状态与启动同步 `useEffect`（依赖 `dataSrc`，数据就绪后执行并 toast 提示） |
- **验证**：`npm run build` ✅（46 模块）；后端启动时进入页面会看到 toast「已连接 FastAPI：页面数据已从后端同步」
- **已知限制（明确记录，避免误以为已完成）**
  - Pulse 摘要数字（3/2/4/6）、Insight 的"原因/下一步建议"、图表数据仍是**页面内置演示语义**；正确归宿是后端 Insight Engine / `GET /api/insights/{id}` 详情扩展
  - `evidence` 内存映射未全量同步（证据抽屉已按 id 走 HTTP）
  - `capabilities` / `autoExpertNotes` 为前端静态演示内容
- **取舍说明**：不继续无脑把整份演示文案搬到后端；等 V2 引擎接管"判断"时，这些字段才会自然成为 API 契约的一部分

---

---

## 迭代 6：V2 引擎起点 —— Metric → Rule → Signal → Insight（纯确定性计算）

- **时间**：2026-09-06
- **目标**：让"判断"真正由代码算出来——从原始经营数据计算指标、规则求值、形成信号、生成洞察与置信度（不含 LLM，见 D009/D014）
- **新增后端结构**
  | 文件 | 说明 |
  |---|---|
  | `backend/app/engine/dataset.py` | V2 原始经营数据（利润率/采购价/退货/订单/客单价/新品/华东/高客单结构），含证据原行输出 |
  | `backend/app/engine/engine.py` | 全链路：`compute_snapshot()` 指标 → `evaluate_signals()` 规则 → `build_insights()` Signal→Insight + 置信度 + Pulse 汇总 → `run_engine()` |
  | `backend/app/engine/__init__.py` | 引擎子包标记 |
  | `backend/tests/engine_check.py` | 引擎自检（无 pytest 依赖，`python3 -m tests.engine_check`） |
  | `backend/app/routers.py` | 新增 `GET /api/engine/run` / `pulse` / `insights`（与原静态端点并存） |
- **与现状的关系（重要）**
  - 现有 `app/data.py` 快照端点（`/api/insights`、`/api/evidence`…）**保持不变**，前端演示继续稳定
  - 引擎产出同形状契约并挂独立 `/api/engine/*`，可对比验证；数值是算出来的（如利润率 18.1%·连续 3 天低于 18.5%、退货 +1.7pp、客单价 ↑4.4%、新品 ↑35.2%）
- **验证**
  - `python3 -m tests.engine_check` ✅（问题 3 / 机会 1 / 变化 3，pulse 与两次运行确定性一致）
  - `curl /api/engine/pulse|run|insights` ✅（200）
- **边界**：叙事文案仍是**模板占位**（V3 Reasoning 替换）；east/new_sku 等证据行部分为占位；规则写在代码里（后续迁 PostgreSQL/配置化）

---

---

## 迭代 7：引擎接入主端点 + 前端联动

- **时间**：2026-09-06
- **目标**：让页面真正消费引擎计算结果（第 1 步落地）
- **后端**
  - `GET /api/pulse` → `run_engine()["pulse"]`（计算值：问题 3 / 机会 1 / 变化 3）
  - `GET /api/insights?type=` → 引擎洞察（机会列表现在只有引擎算出的 1 条）
  - `GET /api/insights/{id}` → 引擎洞察 + `trigger / factors / evidence` 详情；历史 id（o2/c2）静态兜底
  - `GET /api/evidence/{id}` → 引擎即时生成证据链（`engine_evidence()`）；非引擎 id 回退静态
- **前端（计数数据驱动，不硬编码）**
  - `PulsePage`：四张摘要卡片数量改为 `insights.*.length` / `watchItems.length`
  - `InsightPage`：Tab「问题/机会/变化洞察 · N」的 N 改为 `insights[t].length`
- **验证**
  - curl：`/api/pulse` 引擎计数；`/insights?type=opportunity` = 1；`/insights/p1` 含 trigger；`/evidence/p1` 由引擎生成（18.1%·↓5.4%）；`/evidence/o2` 静态兜底 200 ✅
  - 代理链 `5173/api/insights` 返回引擎数据 ✅
  - `npm run build` ✅

---

---

## 迭代 8：D011 收尾 —— 判断语义字段全面上移后端

- **时间**：2026-09-06
- **目标**：把"原因链 / 下一步建议 / Dora 判断"从页面内置语义搬进后端引擎契约
- **后端**
  - 引擎为每条洞察生成 `semantics`：`causeA`（主要因素名+值）、`causeB`（进一步定位名+值）、`next`（3 条下一步，数值全部由快照注入）
  - `GET /api/insights` 改为**原样返回引擎项**（不再经 response_model 过滤掉扩展字段）；`GET /api/insights/{id}` 含 semantics
- **前端**
  - `types.ts`：`Insight.semantics?` + `InsightSemantics`
  - `InsightPage`：原因链两组 box 的名称/数值、下一步建议 → 优先 `x.semantics`，无则静态兜底（离线不破）
  - `PulsePage`：P1 主线 copy / 事实卡 / "Dora 判断" → 有 semantics 时由规则定位生成，仅离线走静态兜底
- **验证**
  - 后端：`semantics attached: 7`；`/api/insights?type=problem` 首条 p1 含 causeA `A 产品线采购成本 · 较期初 2.2%`、next=3 ✅
  - 前端：`npm run build` ✅；InsightPage / PulsePage dev 200 ✅
- **诚实边界**：图表配置与离线 fallback 演示句仍在前端（图表属展示层）；在线（引擎）路径下页面展示的判断字段已全部来自后端

---

---

## 迭代 9（OpenSpec change `extend-engine-coverage`，路线图 C1）：引擎覆盖补齐 c2/o2

- **时间**：2026-09-06
- **目标**：消除 c2（数据更新事件）/ o2（高客单门店集群机会）的静态兜底，全部由引擎计算
- **后端**
  - `dataset.py`：新增 `DS_UPDATE`（09:32 / +327 条 / 受影响指标）与 `STORE_CLUSTER`（华东 7/10、区域客单价、新品渗透 82%）；`rows_for_evidence()` 支持 `data_event`/`store_cluster` 原始行
  - `engine.py`：snapshot 新增 `data_event`/`store_cluster` 指标块；`evaluate_signals()` 新增 `sig-data-event`(c2/change) 与 `sig-store-cluster`(o2/opportunity)；`INSIGHT_TEMPLATES`、`_semantics()`、`EVIDENCE_META` 补齐
  - `tests/engine_check.py`：断言 c2/o2 存在 + pulse = problems 3 / opportunities 2 / changes 4 / watching 4
- **前端**：零改动（计数已数据驱动，自动跟随）
- **验证**：`python3 -m tests.engine_check` ✅；`/api/insights?type=change|opportunity` 含 c2/o2 ✅；`/api/evidence/c2|o2` 引擎生成非占位行 ✅；5173 代理 pulse=2/4/4 ✅；`npm run build` ✅
- **边界**：规则阈值内置（集群机会 60%）；证据深度（SKU/原因）留待后续

---

---

## 迭代 10（OpenSpec change `postgres-repository`，路线图 C2）：PostgreSQL Repository + 规则配置化

- **时间**：2026-09-07
- **目标**：引擎数据源从内存常量切到 PostgreSQL Repository；规则阈值可配置（DB 优先、缺省回退）
- **后端**
  - 新增 `docker-compose.yml`（postgres:16，dora/dora@5432）；`backend/.env.example` + `DATABASE_URL` 读取（app/db.py，惰性连接）
  - 新增 `app/models.py`（metric_series / store_cluster_store / data_update_log / rule_config，均含唯一约束）；`app/repository.py`（读 + 种子 upsert + `DataSourceUnavailableError`）；`app/seed.py`（从 dataset.py 幂等灌库）
  - `engine.py`：新增 `RULE_DEFAULTS`/`_rule_map`/`_series`；`compute_snapshot(repo, rules)` 从 Repository 读取并聚合；`evaluate_signals()` 阈值全部参数化；`run_engine(repo)` 默认 Repository
  - `main.py`：503 exception handler（DB 离线不得回退 Mock）
  - `requirements.txt`：`psycopg[binary]` 3.2.9 → **3.2.13**（3.2.9 无 Python 3.14 wheel）
- **前端**：零改动
- **测试证据**（6.x 门禁）
  - `docker compose up -d` 容器 healthy ✅；psycopg 连 PG16.15 ✅
  - `python -m app.seed` ×2 → 58/10/1/6 幂等不翻倍 ✅
  - `python3 -m tests.engine_check` → pulse 3/2/4/4，metrics 无 `.0` 退化（4,286 / ¥2,985）✅
  - 规则配置化：改 `rule_config` 华东阈值 -3.0→-1.5，c1 触发文案变「已跌破升级阈值 -1.5%…」；改回后恢复「未达…保持观察」；`_rule_map(None)` 缺省回退 ✅
  - DB down 503：`docker compose stop` → `/api/pulse` 503 `{"detail":"data source unavailable"}`；`start` 后 200 ✅
  - `python3 /tmp/dora_smoke.py`（5173 代理）3/2/4/4、9 条 semantics、evidence 非空 ✅；`npm run build` ✅
- **边界**：证据行仍由 `dataset.py` 辅助生成（镜像种子，等价于 DB 内容）；规则为"参数可配置的代码规则"，非全声明式 DSL；迁移/升级工具（Alembic）尚未引入
- **反思**：① 环境先行——Py3.14 与 psycopg 3.2.9 无 wheel 是"切 DB 才暴露"的真实坑，教训：环境差异要在任务 1 就探明；② Repository 的 REAL 列会让整数计数显示成 `4,286.0`——展示契约回归要靠 engine_check 断言兜住（已补）；③ 本轮修复了上轮 c1"跌破阈值静默消失"并补了场景断言，印证"任务自测须对齐 spec 场景"的流程结论

---

---

## 迭代 11（OpenSpec change `fix-new-sku-evidence`，审查整改）：c3 证据链错配修复

- **时间**：2026-09-07
- **目标**：修复审查发现的 c3「新品销量」洞察证据仍显示利润率行的错配
- **改动**：`dataset.rows_for_evidence()` 新增 `new_sku` kind（周/销量/环比）；引擎 `sig-new-sku.evidence_kind` margin→new_sku；`EVIDENCE_META` 加 new_sku；engine_check 补"kind≠margin 且首行含新品销量"断言
- **测试证据**：engine_check OK（pulse 3/2/4/4）；`/api/evidence/c3` rawRows[0]=`['新品销量','第4周','1,820','—']` ✅
- **边界/反思**：证据仍取自种子常量（DB 化并入 C3）；审计其余项（死代码/阈值双源/过期 docstring）已列入路线图"收尾杂项"后置；P009 记录"占位必须配防错断言"教训

---

---

## 迭代 12（OpenSpec change `dataset-upload`，路线图 C3）：真实文件上传入库 + 冷启动入口接通

- **时间**：2026-09-07
- **目标**：打通"上传 csv/xlsx → 校验 → 事务落库 → 引擎即时反映"，并把前端冷启动样例/上传接真实后端（离线回退保留）
- **后端**
  - `requirements.txt` 增 `openpyxl==3.1.5`；`app/repository.py` 增 `replace_series`（同事务删旧+插新）、`delete_all_series`/`delete_all_updates`（出厂重置）
  - 新增 `app/ingest.py`：CSV 标准库 / xlsx openpyxl 解析 + 校验（表头、key 白名单、数值、非空），`IngestValidationError`
  - `routers.py`：`POST /api/datasets`（≤10MB、400/413 语义）、`POST /api/datasets/sample`、`GET /api/datasets/current`
  - `seed.run_seed()` 改为**出厂重置**（先清空 series+update_log 再种入规范种子）
  - 新增 `backend/tests/ingest_check.py`
- **前端**：`doraApi` 增 `uploadDataset/loadSampleDataset`；`DataSourcePicker` 真实调用（离线回退）；Onboarding 文案更新
- **测试证据**
  - `python3 -m tests.ingest_check` ✅（合法解析/非法抛错/替换后 p1 消失/还原恢复）
  - curl：合法 margin=20% 上传 → problems `[p2,p3]`；非法文件 400 `value 非数值`；样例还原 problems `[p1,p2,p3]` ✅
  - `engine_check` OK（pulse 3/2/4/4）；`app.seed` ×2 幂等 58/10/1/6 ✅；`npm run build` ✅；`/tmp/dora_smoke.py` 全绿 ✅
- **边界/反思**：上传为"引擎输入规范格式"（任意口径映射=C4）；证据 DB 化后置；"样例=出厂重置（清空残留）"是 apply 期补充决策（记入 D017）；xlsx 依赖 openpyxl、限制 10MB

---

---

## 迭代 13（OpenSpec change `upload-mapping`，路线图 C4）：上传列口径映射

- **时间**：2026-09-07
- **目标**：任意列布局文件可通过"预览→列映射→入库"进入引擎（承接 Metric Definition）
- **后端**
  - `ingest.py`：抽出 `read_rows`；新增 `preview_rows`（列+样例，不落库）与 `items_from_mapping`（映射校验：key 白名单/列存在/数值/label 非空）
  - `routers.py`：`POST /api/datasets/preview`、`POST /api/datasets/mapped`（file + mapping JSON），复用 400/413/503 语义
- **前端**：`doraApi` 增 `previewDataset/mappedDataset`；`DataSourcePicker` 在线分支选文件→预览→映射表单（metric_key + value/label/dimension 列）→入库；离线回退原行为
- **测试证据**
  - `tests.ingest_check`（含映射用例）✅、`engine_check` ✅
  - curl：preview 返回列与样例；mapped（date/margin_value/region→margin）成功后 problems `[p2,p3]`；样例还原恢复 ✅
  - `npm run build` ✅；冒烟 3/2/4/4 全绿 ✅
- **边界/反思**：映射字段集=单值列契约（组合口径不支持，见 D018）；映射自动推断不做；表单仅在线可用

---

---

## 迭代 14（OpenSpec change `e2e-guardrails`，路线图 C5）：E2E 门禁 + 跌破阈值自动升级

- **时间**：2026-09-07
- **目标**：把文档 4 个核心验收用例固化为可重复跑的 API E2E；补齐 Watch→跌破阈值→自动升级 Problem
- **引擎**：`evaluate_signals` 拆分华东分支——跌破阈值→`e2`(problem，自动升级)，未跌破仍为 c1(change)；补模板/`_semantics`
- **门禁**：新增 `tests/e2e_api_check.py`（4 用例，自重置）与 `tests/run_all.py`（串联 engine+ingest+e2e）；run_all 纳入提交前门禁
- **测试证据**
  - `python3 -m tests.e2e_api_check` ✅（Case1–4：Problem→Evidence→Action；Opportunity→Action；Change→Watch；跌破→e2 自动升级→还原）
  - `python3 -m tests.run_all` → **ALL GREEN** ✅（engine/ingest/e2e 三段）
  - `npm run build` ✅（前端未改，顺带回归）
- **边界/反思**：e2 为最小自动升级（完整 Watch 委托/调度属 V4）；E2E 需后端+DB 在线（README 已注明）；文件上传用系统 curl 规避自拼 multipart 兼容坑（StringIO 位置 bug 也修了）

---

---

## 迭代 15（OpenSpec change `code-cleanup`，审查收尾）：死代码 / 阈值单源 / docstring / 证据 DB 化

- **时间**：2026-09-07
- **目标**：等价重构（行为零变化），清掉审计遗留
- **改动**
  - 前端：删除 `doraApi.isApiLive/liveSet` 死代码
  - 后端：`seed.rule_items()` 由 `engine.RULE_DEFAULTS` 派生（阈值单一来源）；`engine.py` docstring 更新为"Repository + 规则配置化"现状；证据行改为 `_evidence_rows_from_repo` 从 Repository 生成（不再读 `dataset.rows_for_evidence`）
- **测试证据**
  - `tests.run_all` → **ALL GREEN**（engine/ingest/e2e）✅；seed rule_config=6 且值不变 ✅
  - `npm run build` ✅、grep 无 isApiLive/liveSet ✅、冒烟 3/2/4/4 全绿 ✅
- **边界/反思**：`dataset.py` 保留为"种子源"；行为等价由 run_all + 冒烟证明；证据 DB 化后 engine_check 改为经 `engine_evidence` 断言原始行

---

---

## 迭代 16（OpenSpec change `fix-v2-critical-boundaries`，修复分支）：V2 关键边界修复

- **时间**：2026-09-07
- **目标**：修复边界审查 5 项（场景 2/4/6/10/7）
- **后端**
  - 空数据门禁：`run_engine` 无 metric_series → 空 pulse(0/0/0/0, `last_updated='--'`)、空 insights（即使 cluster/update_log 残留）
  - 阈值比较规则化：`_below`（严格 `<`）与 `_breach(actual, threshold, inclusive=True)`（跌破默认 `<=`）；engine_check 加断言
  - 时间动态化：pulse `last_updated` 与 `engine_evidence.updated` 取最新 `data_update`，无事件 `--`
- **前端**
  - Pulse 状态栏改用 `dataLabel`（App 由 `dataSrc` 下发，默认旧文案兼容）
  - "持续关注"计数旁加 `?`（title 悬浮"当前为静态展示，真实动态委托将在 V4 上线"）
- **测试证据**
  - `python3 -m tests.run_all` → ALL GREEN ✅；端点空数据场景：`/api/pulse`=0/0/0/0+`--`、`/api/insights`=[] ✅；`npm run build` ✅
- **边界/反思**：east 阈值"含等"语义已固化并有断言；Watch/Action 静态属 V4（RELEASE_NOTES_V2 脚注）；时间戳日期用当天（事件仅存 HH:MM）

---

---

## 迭代 17（OpenSpec change `v3-reasoning-layer`，路线图 V3-T1）：推理抽象层

- **时间**：2026-09-07
- **目标**：V3 文本层解耦的地基——provider 协议 + 模板默认实现 + 配置解析 + parity 测试（零行为变化）
- **后端**
  - 新增 `app/reasoning/`（`__init__.py`、`provider.py`）：`ReasoningContext/Result`、`ReasoningProvider` 协议、`TemplateProvider`（默认，语义与引擎模板一致）、`resolve_provider()`（`DORA_REASONING_PROVIDER`，未知值抛错）
  - `backend/.env.example` 追加 provider 变量
  - `tests/reasoning_check.py`（parity：TemplateProvider==引擎 semantics，覆盖 9 条洞察）
  - `tests/run_all.py` 纳入 reasoning_check
- **测试证据**：reasoning_check OK（parity 9）；run_all **ALL GREEN**（4 段）；`npm run build` ✅
- **边界/反思**：引擎尚未接线（T2 语义缓存时收编）；TemplateProvider 延迟导入引擎私有 `_semantics`，避免启动循环

---

---

## 迭代 18（OpenSpec change `v3-semantics-cache`，路线图 V3-T2）：语义缓存 + 来源标注

- **时间**：2026-09-07
- **目标**：把"解释语义"接入缓存管线；D023 退出（semantics 收编进 reasoning 包）
- **后端**
  - 新增 `insight_reasoning` 表（insight_id 唯一 / semantics JSON / provider / generated_at）与 Repository 读写
  - `_semantics` 迁移为 `app/reasoning/semantics.py::template_semantics`；引擎留 `_semantics` 别名；`TemplateProvider` 改从 reasoning 包读取（不再依赖引擎私有函数）
  - 新增 `app/reasoning/cache.py::refresh`（幂等写缓存，返回 updated/fallback）
  - `run_engine` 合并缓存：命中→覆盖 semantics + `reasonSource`/`generatedAt`；未命中→`reasonSource="template"`（不写缓存）
- **测试证据**：reasoning_check（parity 9 + T2 缓存合并断言）✅；run_all（4 段）ALL GREEN ✅；端点验证：缓存命中 `p1 reasonSource=template generatedAt=2026-09-07T…`；清库后 `template` 且无 generatedAt ✅
- **边界/反思**：缓存无失效策略（数据集变更后旧解释仍在，T3/T5 提供 refresh 与指纹失效）；N+1 查询（≤9 条可接受）

---

---

## 迭代 19（OpenSpec change `v3-llm-provider`，路线图 V3-T3）：LLM Provider + refresh API + 缓存失效

- **时间**：2026-09-07
- **目标**：把 LLM 解释接入（OpenAI 兼容）+ 暴露刷新入口 + 兑现 D024 退出（数据写入即失效）
- **后端**
  - `app/reasoning/llm.py`：LLMProvider（chat/completions、JSON-only、**数值稳定合并**：value 永远取引擎模板基线，name/next 可改写且结构校验）；无 key 抛错→fallback
  - `resolve_provider` 支持 `llm`；未知 provider 抛错
  - `POST /api/reason/refresh`：批量刷新返回 `{ok, updated, fallback, provider}`（幂等）
  - 失效：`POST /api/datasets`、`/datasets/mapped`、出厂重置（seed/sample）清空 `insight_reasoning`
  - `.env.example` 补 LLM 变量
- **测试证据**：reasoning_check（无 key fallback 全量、数值稳定 mock、非法 next 回退）✅；run_all（4 段）ALL GREEN ✅；端点：refresh→rows=9；sample→rows=0 且 reasonSource=template ✅
- **边界/反思**：LLM 质量靠 JSON-only+数值锁保证；全清缓存粒度粗（≤9 行可接受）；同步批量（量大再异步）

---

---

## 迭代 20（OpenSpec change `v3-reasoning-ui`，路线图 V3-T4）：前端"重新解释"入口 + 来源徽标

- **时间**：2026-09-07
- **目标**：让解释来源可见、可刷新
- **前端**
  - `doraApi.refreshReasoningApi()`（三模式）；`types.ts` Insight 增可选 `reasonSource/generatedAt`
  - `App.refreshReasoning`：refresh→同步→重渲染→toast（llm/template/失败回退）；传入 InsightPage
  - `InsightPage`：详情头部来源徽标（AI 解释 / 模板解释）+ "↻ 重新解释"（busy 态）
- **测试证据**：`npm run build` ✅；`/api/insights` p1 `reasonSource=template` + semantics ✅；冒烟 3/2/4/4 全绿 ✅；后端未动（run_all 参照绿）
- **边界/反思**：徽标值来自后端 `reasonSource`（T2 合并），本地 data.ts 演示数据无该字段 → 显示"模板解释"属合理回退

---

---

## 迭代 21（OpenSpec change `v3-acceptance`，路线图 V3-T5）：验收与收尾

- **时间**：2026-09-07
- **目标**：把 V3 验收固化（金样本 + 红线断言入 5 段门禁）并产出人工审查清单
- **交付**
  - `tests/golden_baseline.json`（V2/V3-template 模板快照：pulse + 9 条洞察判定字段与 semantics）
  - `tests/golden_check.py`（`--write` 可重生成；比对不一致即失败）
  - `tests/run_all.py` 增 golden_check → **5 段门禁**
  - `docs/V3_ACCEPTANCE_CHECKLIST.md`：§3A.4 自动项命令 + 人工核对项 + sign-off
- **测试证据**：golden_check OK（9 insights matched baseline）；run_all **ALL GREEN（5 段）**；前端 build/冒烟绿
- **边界/反思**：基线随语义演进需按变更流程 `--write` 重生成并评审；红线（LLM 不改数字）由 reasoning_check+golden 双保险

---

## 迭代 22（OpenSpec change `v4-watch-domain`，路线图 V4-T1）：Watch 领域与持久化（V4 地基）

- **时间**：2026-09-07
- **目标**：把"持续关注"委托变成可持久化的真实对象（V4 数据层地基；解析/评估/前端/升级 E2E 分别留给 T2–T5）
- **交付**
  - `models.py`：新增 `WatchTarget`（w-+uuid id、原文 raw_text、解析结果 intent JSON、status watching|paused、frequency on_update|daily 09:00|weekly、时间戳）与 `WatchEvent`（归属 target、kind change|escalate、summary、前后值 values JSON）
  - `repository.py`：CRUD 原语（create/list/get/set_status/delete 级联清事件·幂等/delete_all/add_event/list_events）+ 枚举校验早暴露 + `count_rows` 补两表
  - `tests/watch_check.py` 第 1 节（领域 CRUD 往返）；`tests/run_all.py` 增 watch_check → **6 段门禁**
  - 新建能力 spec 首块：`openspec/specs/business-watch`（Requirement「委托与命中事件可持久化存取」+ 2 场景）
- **测试证据**：watch_check OK（第 1 节 领域 CRUD 全绿）✅；run_all **ALL GREEN（6 段**：engine/ingest/reasoning/golden/e2e/watch）✅；golden 9 insights 不变 ✅
- **边界/反思**：CRUD 契约先于 API（T3/T4）落地，用 watch_check 固化为锚点防返工；出厂重置是否清委托留 T3 接线时决策（倾向保留目标清事件）；ORM 沿用无 relationship 风格，级联在 Repository 事务内显式执行

## 迭代 23（OpenSpec change `v4-watch-parser`，路线图 V4-T2）：委托解析与确认（词典 + 显式拒绝）

- **时间**：2026-09-07
- **目标**：把"一句话委托"结构化（D029：不接 LLM 判定，词典 + 显式确认），并暴露 parse 端点供 T4 确认回显
- **交付**
  - `app/watch/parser.py`：`parse_watch_text` → `{ok, intent?, unsupported[]}`；TARGETS 有序指标词典（8 key × 别名 × 默认 dimension × label）；条件句式（连续 N 天降/涨、跌破/低于/超过/高于 X(%)，breach 优先单条件）；频率词（on_update/daily 09:00/weekly）；缺条件给建议默认并标 `condition_defaulted`
  - `POST /api/watch/parse`（WatchParseRequest，纯文本不触 DB）
  - `tests/watch_check.py` 第 2 节「解析」：demo 句 / 跌破+频率 / 中文数字 / 拒绝句式 / 建议 chips 5 支持 1 拒绝
- **测试证据**：watch_check（第 1+2 节）ALL GREEN ✅；run_all **ALL GREEN（6 段）** ✅；端点冒烟（绕代理）：支持句式 ok=true+intent、拒绝句式 ok=false+unsupported ✅
- **边界/反思**：dimension 诚实默认（returns 门店级维度→""，聚合口径 T3 定）；单条件语义（复合条件 T3 评估再扩 schema）；自由语句覆盖率有限，靠建议 chips + 拒绝原因引导；"建议默认条件"必须带 `condition_defaulted` 标记走确认界面，不留隐藏默认

## 迭代 24（OpenSpec change `v4-watch-evaluator`，路线图 V4-T3）：通用评估器 + 触发时机

- **时间**：2026-09-07
- **目标**：让委托真正被评估——条件命中写事件、升级引用引擎判定、数据更新即时触发 + 每日/每周周期调度
- **交付**
  - `app/watch/evaluator.py`：`_chrono_values`（单 dim 直取 / dimension="" 按 label 跨维度均值聚合时间线）、`_eval_condition`（streak 日环比、below/above 最新值）、`evaluate_one`、`evaluate_all`（run_engine 共享一次、去重最近事件 kind+values）；**升级白名单 ESCALATION{margin:p1,returns:p2,orders:p3,aov:o1,east_orders:e2}**，仅引擎有该 insight 才 escalate 并引用 `engine_insight`
  - `app/watch/scheduler.py`：`_is_due` 纯函数（daily 09:00/weekly，last_checked 防重复）+ 单例守护线程每 60s tick（启动补跑）
  - `repository.py`：`touch_watch_target` / `delete_all_watch_events`；`main.py` lifespan 启调度；`routers.py` 三处数据写入后即时评估（异常仅告警）；sample 重置保留委托清事件
  - `tests/watch_check.py` 第 3 节「评估器」（升级引用/去重/未命中/到期判断注入 now/恢复出厂）
- **测试证据**：watch_check（第 1–3 节）ALL GREEN ✅；run_all **ALL GREEN（6 段）** ✅；冒烟：uvicorn lifespan 起 scheduler、sample 200 无评估告警 ✅
- **边界/反思**：求值口径与引擎同源；dimension="" 聚合口径为自定义、watch_check 固化；升级引用引擎 insight 是"不伪造身份"红线代码化；出厂重置语义补定（保留委托清事件）；即时评估 try/except 不阻塞上传主流程

## 迭代 25（OpenSpec change `v4-watch-ui`，路线图 V4-T4）：Watch/Pulse 前端真实化 + watch REST

- **时间**：2026-09-07
- **目标**：把 V4 委托从"能存能算"变成"界面可用"——后端补 watch REST，前端 Watch 页/Pulse 消费真实委托
- **交付**
  - 后端：`GET /watch`（DB 委托→WatchItem 兼容卡片：name/value/color/logic/source + status/frequency/lastEventAt）、`POST /watch`（parse 400 detail→create→即时评估）、`GET /watch/{id}`（含事件）、`PATCH` status、`DELETE`、`POST /{id}/check`；`evaluator.evaluate_one` 单查惰性 `run_engine`（修复单目标升级判定遗漏）
  - 前端：`WatchItem` 增可选字段；doraApi 增 parseWatch/createWatch/setWatchStatus/deleteWatch/checkWatch（业务 4xx 显式上抛不静默 mock）；WatchPage 真实化（parse 回显→确认创建→列表 暂停/恢复/删除/检查）；Pulse 计数实时 + 文案去静态声明
- **测试证据**：`npm run build` ✅（tsc + vite）；run_all **ALL GREEN（6 段）** ✅；watch REST 冒烟：create 200（华东销售额/watching）→list 1 条→pause（已暂停）→check（change）→unsupported 400 detail→delete ok ✅
- **边界/反思**：卡片=WatchItem 超集 → Pulse/Topbar 消费 watchItems 零改动；创建即评估让"创建就有初判"；本次 405 是进程未重载旧代码所致（排障先确认进程加载新代码）；4xx 拒绝必须显式上抛而断网/5xx 才走 mock，防止不支持指标被静默创建

## 迭代 26（OpenSpec change `v4-watch-acceptance`，路线图 V4-T5）：升级路径 E2E + V4 验收收尾

- **时间**：2026-09-07
- **目标**：把文档 Case 3/4 的**委托升级路径**固化为 HTTP 门禁，并产出 V4 验收清单（sign-off 后把 V4 标为完成）
- **交付**
  - `tests/watch_check.py` 第 4 节「升级 E2E（HTTP）」：Case 3 创建 east_orders 委托→change 事件；Case 4 上传跌破阈值→引擎 problem e2 + 委托 escalate（`engine_insight==e2`）+ 卡片"已升级"；清理还原
  - `docs/V4_ACCEPTANCE_CHECKLIST.md`：A 自动门禁（run_all 6 段/build/冒烟，已全 ✅）／B 边界审查（5 项记录 ✅）／C 人工界面核对（待用户）／D 收尾 + Sign-off 表
- **测试证据**：watch_check（第 1–4 节）ALL GREEN ✅；run_all **ALL GREEN（6 段）** ✅（golden 9 不变、e2e 4 case）；build ✅
- **边界/反思**：E2E 断言"存在性"降低对 seed 形状脆弱性；验收清单 C 段必须用户本人确认后再 Sign-off（不代填）；V4 五任务全归档 → 版本速览 V4 = 🔶 已实现、待人工验收

## 迭代 27（OpenSpec change `llm-refresh-policy`，P011/D028 阶段 1）：LLM 刷新兜底（超时/预算/并发/熔断/优先级/单飞）

- **时间**：2026-09-07
- **目标**：让 refresh 在外部供应商故障下**响应有上界**（预算 20s + 单条 ≤10s + 并发 2–3）、故障快速隔离（熔断）、预算给重要洞察、连点不重复打供应商
- **交付**
  - `app/reasoning/policy.py`：env 参数 + `CircuitBreaker`（线程安全、注入时钟可测）+ `DegradationError/CircuitOpenError` + 降级分类（HTTPError 先于 URLError 判定）+ 优先级 helper
  - `llm.py`：超时 40→10s；熔断开启不触网；网络降级记熔断、本地/解析错误不计
  - `cache.py` refresh 重写：llm 走预算/并发/熔断/优先级调度（problem→opportunity→change），连续降级≥3 中止整批剩余 fallback；template 保持串行原语义；契约不变
  - `routers.py`：reason_refresh **单飞**（并发请求合并为一次执行）
  - reasoning_check 补策略单测；run_all 环境钉死已在迭代 26 后落地
- **测试证据**：reasoning_check（policy OK：熔断时钟/优先级/降级分类）✅；run_all **ALL GREEN（6 段）** ✅（golden 9 不变）；真实 DeepSeek 批量 9/9 updated **4.9s**（并发 3，串行时代 15.6s）；**单飞冒烟**：并发两请求 wall 5.4s 均返回 9 updated（仅执行一批），updated 顺序 = 优先级 ✅
- **边界/反思**：urllib 无独立 connect 超时 → 总超时 10s 落地（见 D030）；HTTPError⊂URLError 判定顺序坑由单测当场捕获；无 key/解析错不计熔断防配置错熔掉全批；单飞仅进程内生效

## 迭代 28（OpenSpec change `v5-action-domain`，路线图 V5-T1）：Action 领域与持久化（V5 地基）

- **时间**：2026-09-07
- **目标**：把"加入行动回路"升级为真实 Case 状态机的数据层地基；5 例静态 ACTIONS 迁移 DB 种子
- **交付**
  - `models.py`：`ActionCase`（id=insight_id、kind/code/case_title/status open|running|waiting_verify|resolved、orchestration JSON、archive、时间戳）+ `ActionStep`（case_id、seq、title/desc/evidence/why、status pending|in_progress|done|blocked、note/result、finished_at）
  - `repository.py`：建案（单事务含首步集）/upsert_seed 幂等迁移/list/get（含 steps）/set_action_step_status（done 记 finished_at）/verify（resolved|continue）/delete 级联·幂等/delete_all + count_rows 两表
  - `seed.py`：`action_cases_from_static` 把 5 例拍平（前 2 done / current in_progress / 后 2 pending，保留 PRF-0831 等 code、编排、archive），幂等
  - `tests/action_check.py` 第 1 节（seed 幂等 + CRUD 往返）；run_all 增 action_check → **7 段**
- **测试证据**：action_check（第 1 节）ALL GREEN ✅；run_all **ALL GREEN（7 段：…/watch/action）** ✅（golden 9 不变）
- **边界/反思**：静态 ACTIONS 为 camelCase 字段（tagCls/caseTitle），seed 映射用 snake 时踩 KeyError——读源字典实际键再映射；case id=insight_id 为 T2 幂等建档打底；状态机合法性规则留给 T3 服务层（repo 只校验合法值+记时间）

## 迭代 29（OpenSpec change `v5-action-builder`，路线图 V5-T2）：Case 建档引擎

- **时间**：2026-09-07
- **目标**：把引擎判定的 problem/opportunity 洞察自动转成可执行档案（幂等、仅引擎判定可建档）
- **交付**
  - `app/action/builder.py`：`create_case_from_insight`——当前 run_engine 快照校验（type∈{problem,opportunity} 且 id 命中，change/伪造/watch escalate 拒绝）；已有档案返回 created=False；4 步模板（发现 done→定位/拆解 in_progress→验证/归档 pending，desc/evidence 引用 trigger/metric/delta）；code 确定性 PRB-/UPC- + sha1[:4]
  - `tests/action_check.py` 第 2 节「建档引擎」
- **测试证据**：action_check（第 1+2 节）ALL GREEN ✅；run_all **ALL GREEN（7 段）** ✅（golden 9 不变）
- **边界/反思**："引擎判定集内才可建档"落在执行路径（run_engine 快照校验）而非文档；幂等双保险（builder 查档案 + repo.create 抛同 id 兜底）；新 code 与静态 seed code 并存不冲突

## 迭代 30（OpenSpec change `v5-action-api`，路线图 V5-T3）：执行/验证状态机 API

- **时间**：2026-09-07
- **目标**：把 Case 变成可推进的状态机（start/done/blocked + verify resolved/continue），并暴露 `/api/action/cases` REST；非法迁移 4xx
- **交付**
  - `app/action/flow.py`：合法性唯一真源——start（仅 pending|blocked→in_progress）/done（仅 in_progress→done，note/result 回填，全 done→waiting_verify）/blocked/verify（resolved 需全 done+note 入 archive；continue→running）；resolved 冻结一切推进
  - `repository.py`：verify 支持 note 追加 archive、新增 set_action_case_status
  - REST `/api/action/cases`：POST 建档=builder（仅当前引擎 problem/opportunity）、GET list/detail、`steps/{seq}/start|done|blocked`、`verify`（400/404 语义）；旧 `/api/actions` 静态保留（T5 下线）
  - schemas 三模型；`tests/action_check.py` 第 3 节（状态机服务）
- **测试证据**：action_check（第 1–3 节）ALL GREEN ✅；run_all **ALL GREEN（7 段）** ✅（golden 9 不变、e2e Case1/2 契约不变）；API 冒烟：未跌破建 e2→400；跌破建档 200；pending 直 done→400；推进→waiting_verify→resolved→再 verify 400 ✅
- **边界/反思**：状态机合法性唯一真源在 flow.py（repo 只管合法枚举）；verify note 追加 archive 避免给已有表加列（create_all 无迁移能力）；resolved 冻结由统一 guard 拦；旧 /actions 静态与真 REST 并存期到 T5 前端切换后下线

## 迭代 31（OpenSpec change `v5-action-ui`，路线图 V5-T4）：Action 前端真实化（真数据源）

- **时间**：2026-09-07
- **目标**：Action 页从 data.ts 静态切换到 /api/action/cases 真档案；洞察「加入行动回路」= 真建档
- **交付**
  - types：CaseStep/ActionCaseDetail；doraApi：createAction/listActionCases/fetchActionCase/startStep/doneStep/blockStep/verifyAction（4xx 显式上抛）
  - App.route：problem/opportunity → createAction 建档成功后跳转（400 展示 detail 不伪造；断网才回退本地 join 演示）
  - ActionPage 重写：真档案列表/详情/步骤状态按钮/验证区（归档 note / 继续）/resolved 归档视图/专家编排；后端不可用 → demo 双 return 兜底
- **测试证据**：`npm run build` ✅（tsc+vite）；run_all **ALL GREEN（7 段）** ✅（golden 9 不变）；交互路径走真端点（页面冒烟依赖运行界面）
- **边界/反思**：旧 doraApi 已有同名 getActionCase（/actions 静态）→ 新函数命名冲突被 tsc 抓住，改 fetchActionCase 避坑；demo/真实双 return 利于类型收窄；视觉从静态时间线简化为通用步骤列表（功能真实优先）

## 迭代 32（OpenSpec change `fix-reason-refresh-timeout`，Bug1 收口）：前端 reason refresh 超时 2600→30000

- **时间**：2026-09-07
- **问题**：接真实 DeepSeek（批量 ~4.9s、预算 20s+尾差）后，前端 `refreshReasoningApi` 走默认 **2600ms** 必 abort → 每个 problem「重新解释」都 toast「解释刷新失败，已保持模板解释」，而后端其实跑完并写 llm 缓存（UI 自相矛盾）
- **修复**：`refreshReasoningApi` 显式 `timeoutMs = 30000`（对齐后端预算上界 + 余量；单飞防连点重复打 LLM）
- **测试证据**：`npm run build` 通过；实测 LLM 批量 4.9s << 30s → 不再误杀 ✅
- **反思**：P011 早在分析期标记 Bug1，接 key 后应立即兑现并把「重新解释」点一遍纳入验收；前端假失败 + 后端已写缓存 = 状态自相矛盾，靠超时对齐消除（阶段 2 异步化才是终态）

## 迭代 33（OpenSpec change `v5-action-acceptance`，路线图 V5-T5）：闭环 E2E + V5 验收收尾

- **时间**：2026-09-07
- **目标**：把文档第 34 章 Action 闭环（升级→建档→执行→验证→归档 / 机会 continue）固化为 HTTP 门禁并产出 V5 验收清单
- **交付**
  - `tests/action_check.py` 第 4 节「闭环 E2E（HTTP）」：Case4+1（watch→跌破→引擎 e2→建档 created→全 done→resolved note 入 archive→再 verify 400）＋ Case2（o1 幂等→推进→waiting→verify continue→running）；自清 + sample 还原
  - `docs/V5_ACCEPTANCE_CHECKLIST.md`：A 自动（全 ✅）／B 边界（记录 ✅）／C 人工界面核对（待用户）／D 收尾 + Sign-off
- **测试证据**：action_check（第 1–4 节）ALL GREEN ✅；run_all **ALL GREEN（7 段）** ✅（golden 9 不变）
- **边界/反思**：E2E 断言"存在性+终态"（中间细节第 3 节已覆盖）；HTTP 预期 4xx 用 HTTPError 捕获断言；sample 不重置 action 档案→测试自清；watch escalate 不自动建档、引擎 problem 才可建档的红线在端到端链路得到验证

## 迭代 34（OpenSpec change `cleanup-static-layer`，P0-1）：静态双源下线 + 死代码/DTO 清理

- **时间**：2026-09-07
- **问题**：审计（docs/优化方向.md）发现服务端仍残留静态双源（/actions\*、/insights 历史兜底、/evidence 兜底）、前端死代码、Action 列表"摘要冒充详情"
- **交付**
  - 后端：移除 /actions list/get/execute、/insights/{id} 静态兜底、/evidence 兜底；删 routers 对 app.data 的引用；get_insight/evidence 只认引擎/Repository（缺则 404）
  - e2e Case1/2 改为真实 /action/cases/p1|o1 档案断言
  - 前端：删 createFollowup/executeAction/旧 getActionCase/listActions；syncRemoteData 不再拉 /actions（data.ts 演示原样）；DTO 拆型 ActionCaseCard（摘要）/ ActionCaseDetail（详情）；ActionPage 列表/详情分离
  - .env.example 补 DeepSeek 注释样例（F10）
- **测试证据**：run_all **ALL GREEN（7 段）** ✅（e2e Case1/2 真实档案断言，golden 9 不变）；npm build ✅；curl：/api/actions→404、/api/actions/p1→404、/insights/problem 200、/action/cases 200、/evidence/p1 200 ✅
- **反思**：删静态前先改消费方（e2e/sync）再删端点；诚实兜底=缺则 404 暴露缺口而非静默回退演示；列表/详情拆 DTO 消除类型谎言；data.ts 定位=离线演示

## 迭代 35（OpenSpec change `fix-audit-f4f9`）：审计问题收口（F4/F5/F6/F7/F9）

- **时间**：2026-09-07
- **问题/收口**：docs/优化方向.md 审计问题处置
  - F4：seed 档案"仅缺省建档"（sample 归零的是数据不是已推进档案/验证记录）；测试自复位（o1/o2）保证门禁可重复
  - F5：新增 `tests/llm_real_smoke.py`（无 key SKIP、不进 run_all）
  - F6：成文语义（重启按 last_checked 补跑；多进程前换 Redis/Celery）
  - F7：Watch/Action 页 20s 轻量轮询（真实模式）
  - F9：数字一致性摸底通过（9 insights evidence 全覆盖、Pulse counts=列表 counts）；新发现"图表静态"单列 F11
- **测试证据**：action_check 第 1–4 节连跑两次 ALL GREEN ✅；run_all 连跑两次均 **ALL GREEN（7 段）** ✅；npm build ✅；llm_real_smoke（真实 DeepSeek）单条 1.8s + 全量 9/9（5.3s），数值锁通过 ✅
- **反思**：行动档案是可追溯记录（V5 红线），演示数据重置不得抹掉进度——修复方向=保留而非"重建基线"；测试用自复位替代依赖 sample 复位；轻量轮询先解痛，正式推送留阶段 2；F11 诚实暴露"图表仍是静态演示数据"

## 迭代 36（OpenSpec change `action-lesson-archive`，Action 产品升级）：抽屉布局 + 归档经验沉淀

- **时间**：2026-09-08
- **目标**：行动回路收尾产品化——档案列表抽屉化让流程主区占大布局；resolved 后归档为**用户可见问题档案（处理过程）**并把可复用经验沉淀进 **CaseLesson 经验库（自我学习）**
- **交付**
  - 后端：`case_lesson` 表（case_id 幂等、code/kind/title/archive 处理过程/resolution 结论）+ Repository（create-if-absent/list/delete）+ `POST /api/action/cases/{id}/archive-as-lesson`（仅 resolved、note 必填、幂等）+ `GET /api/action/lessons`
  - 前端：ActionPage **抽屉布局**（档案列表可收起为竖向 tab，流程主区占大布局）；resolved 视图新增「沉淀经验 → 经验库」；抽屉底部「学习经验 N」查看最近经验
  - spec：business-action +1 Requirement
- **测试证据**：action_check 第 1–5 节 ALL GREEN；run_all 连跑两次 **ALL GREEN（7 段）**；npm build ✅；API 冒烟：running→400、resolved 沉淀 created=True（UPC-0418）、重复 created=False、lessons 200 ✅
- **反思**：经验沉淀用确定性记录（不夸大训练）；resolved-only + 幂等由端点 guard + 主键双保险；UI 以"档案为入口、流程为主角"组织

## 迭代 37（OpenSpec change `fix-action-layout-flex`）：Action 主区布局修复（抽屉化真正生效）

- **时间**：2026-09-08
- **问题**：迭代 36 后用户反馈"流程主区没变大"——根因是 ActionPage 仍把抽屉+主区放进遗留 `.action-layout`（旧双卡 grid `1.65fr/1fr`），抽屉被当宽列、主区被压进 1fr
- **修复**：真实布局换 **flex 容器**：抽屉定宽（开 300/收 60，flexShrink:0 + 过渡），主流程 `flex:1; minWidth:0` 占满剩余大部分
- **测试证据**：`npm run build` ✅（变更仅 ActionPage.tsx 布局容器）
- **反思**：复用遗留 CSS 类名前先确认含义；抽屉化需语义正确的 flex 容器而非塞进旧 grid

## 迭代 38（OpenSpec change `knowledge-archive`）：知识/归档库（按类型入库 + 前端知识库页）

- **时间**：2026-09-08
- **目标**：把归档内容按类型放进"数据库"——resolved 档案（problem/opportunity）与经验（lesson）自动写入统一 `knowledge_archive`；前端新增「◈ 知识库」页按类型筛选
- **交付**
  - 后端：`knowledge_archive` 表（(entry_type,source_id) 唯一幂等；problem|opportunity|change|lesson）；写入钩子（flow.verify resolved→按 case.kind 自动入库；create_case_lesson 同事务补 lesson 行）；`GET /api/knowledge?type=`（entries+stats）；action_check 第 6 节
  - 前端：类型 KnowledgeEntry/Stats；doraApi listKnowledge；新页 KnowledgePage（chips 带计数：全部/问题/机会/变化/经验，条目卡=code/title/结论/处理过程）；Sidebar/App/Topbar 接入 Page 'knowledge'
- **测试证据**：action_check（第 1–6 节）ALL GREEN；run_all **ALL GREEN（7 段）**（golden 9 不变）；npm build ✅；API 冒烟：resolved→problem 行、lesson 行、stats {problem:1,lesson:1}、type 过滤 ✅
- **反思**：knowledge_archive=统一归档视图、case_lesson=经验引用视图（双表冗余留未来合并）；写入点在语义发生处保证一致；action_check 自持 seed 基线（F4 后 sample 不归零）

## 迭代 39（OpenSpec change `frontend-loop-pulse`，持续优化 §3D.1-W1）：Pulse 页真实化收口

- **时间**：2026-09-09
- **目标**：Pulse（业务脉搏总览）不再组件内写死业务文案/伪造跟进——主区/信号板/监控卡全部表达引擎 `/api/insights` + `/api/watch` 同源数据；「让 Dora 帮我跟进」接真实 Watch 委托；离线演示镜像迁移（删字典不删 demo）。
- **交付**（后端零改动，`.openspec.yaml skip_specs: true`）
  - 前端新增 `pulseView.ts`：字段盘点头注释（design D6）+ `heroFor/signalsFrom/monitorsFrom` + `followTargetFor/followedTargetOf`（margin 别名表=唯一受控前后端重复，D5）
  - `PulsePage.tsx` 全重构：删 `view[p1|p2|p3]` 写死字典、`p.id==='p1'` 分支、静态 signal/monitor 数组、PRF-0831 伪造跟进卡；默认焦点=第一个引擎问题（clamp/空态，review P1-2）；「离线演示」标识；跟进/取消对称离线 guard（review P1-3/P1-4）+ `syncRemoteData` 同源刷新；AskBar 保留（F8 不夹带，review P1-5）
  - `data.ts` 离线镜像迁移：demo 洞察 p1–p3 补结构化 semantics/evidence、`watchItems` 类型升级 `WatchTargetCard[]`（审查补强 1/2）
  - `types.ts`：Insight 扩展可选 `trigger/factors/evidence`
  - `docs/持续优化路线.md`：§3D.1 接口标注「/api/pulse 仅纯计数、面板走 /api/insights + /api/watch」（P3-8，防 W3 误用）
- **测试证据**
  - `cd frontend && npm run build` ✅（tsc -b + vite，48 modules）
  - curl：parse「关注利润率，连续 3 天下跌提醒我」→ `ok=true / metric_key=margin / condition={streak_below,days:3} / condition_defaulted=false`；POST 建档 `w-114962088237` → `GET /api/watch` 可见 → DELETE ✅
  - 静态断言：`grep -nE 'const view|p[0-9]|PRF-0831' PulsePage.tsx` 无命中（exit=1）；`grep -nE '/api/pulse' PulsePage.tsx` 无命中；AskBar 渲染 1 处
  - esbuild 离线镜像自检：`heroFor(p1)` facts=2、follow 文案精确、monitors=6/6 → OK
  - `PATH=/Users/tomara/miniforge3/bin:$PATH python3 -m tests.run_all` → **run_all: ALL GREEN**（reasoning/golden 9/e2e 4/watch 4 节/action 6 节）；`openspec validate --changes` 1 passed
  - 人工浏览器双态冒烟（用户执行）✅：在线面板/计数一致、跟进真实落库、取消生效；停后端=demo 观感一致 +「离线演示」提示 + 跟进/取消不假成功
- **反思**：Pulse 的"真实"分两层——计数早已真（D008 原位同步），**文案与子面板才是静态重灾区**；跟进按钮本质是把问题心智转成委托，只对可委托 metric（margin）开放、离线显式诚实。本机 `python3` 是 homebrew（无 sqlalchemy）而服务跑在 miniforge：`tests.run_all` 硬编码 `python3` 需 `PATH` 指到 miniforge（B1 backlog 待改 `sys.executable`）；验证断言先原样跑一遍、避免反斜杠转义与 `/pulse` 子串误匹配 `./pulseView`（P012）。


## 迭代 40（OpenSpec change `knowledge-lifecycle`，Phase0 收口）：生命周期级联 + 单事务 + TEXT + run_all 解释器

- **时间**：2026-09-09
- **目标**：2026-09-08 代码审查 Phase 0（持续优化路线 §5 A1/A2/A3 + §6 B1）收口——`knowledge_archive`/`case_lesson` 与 `action_case` 生命周期一致；resolved 知识入库单事务；archive 扩 TEXT；run_all 解释器一致；顺带清理演示库 o2/p2 孤儿脏行。
- **交付**（business-action spec +1 Requirement「归档知识随档案生命周期一致」）
  - A1：`delete_action_case`/`delete_all_action_cases` 同事务级联删 action_step + case_lesson + knowledge（source_id）；action_check 新增生命周期不变式（main 开头自愈清理 + 结尾断言孤儿=0 + stdout 计数留痕）
  - 补充①：`upsert_seed_case` 不得倒退 resolved 案（已 resolved 档案跳过覆盖，历史/知识/经验保留）
  - A2：`Repository.verify_action_case` resolved 分支单 session 完成置态 + archive 追加 + knowledge 入库；`flow.verify` 删除二次 add_knowledge
  - A3：`action_case.archive`、`case_lesson.archive/resolution`、`knowledge_archive.content/note` 改 Text；`db.init_db()` 幂等 ALTER（PG 方言，重复 no-op）
  - B1：`tests/run_all.py` STEPS 子进程 `"python3"` → `sys.executable`（docstring 说明依赖解释器）
- **测试证据**
  - 功能断言脚本全部通过：`[1.1] resolved 单事务原子 OK`（resolved 返回即知识行存在）、`[1.2] 幂等 OK`、`[2.1] 删除级联 + 幂等 OK`、`[2.3] upsert 不倒退 resolved OK`、`[3.2/3.3] 长 archive OK | len=686 knowledge_content_len=686`、`[3.2] TEXT 迁移幂等 OK（init_db 两遍，5 列均 text）`、`delete_all 级联 OK（已 run_seed 还原基线）`
  - 清理留痕：人为造孤儿后 action_check 自愈输出 `[lifecycle] self-heal removed orphan rows: knowledge=3, case_lesson=2`；post-state lessons=0/knowledge=0、5 个 seed case 完好
  - `action_check OK（第 1–6 节 + 生命周期不变式 … 全绿）`；`run_all: ALL GREEN`（miniforge 解释器直跑，无 PATH hack）；`openspec validate --changes` 通过
- **反思**：A1/A2/A3 本质都是"写路径缺事务边界 + 缺生命周期契约"——最小闭环=单事务 + Repository 显式级联 + 门禁不变式（自愈），不引迁移框架/外键级联；长文本正确做法是 TEXT + init_db 幂等 ALTER 而非 VARCHAR 放大；run_all 用 sys.executable 消除解释器歧义（P012/B1）；demo 库孤儿来自旧版测试 resolve 后基线复位未清残留，门禁自愈从此根治。


## 迭代 41（OpenSpec change `frontend-loop-insight`，持续优化 §3D.1-W2）：Insight 页静态回退字典下线

- **时间**：2026-09-09
- **目标**：Insight 详情页不再按洞察 id 写死归因/下一步——cause/next 只消费引擎下发的 `semantics`（reasoning 缓存/模板）；无 semantics 时显式「模板未覆盖」并只展示引擎原文字段；离线镜像补齐 9/9（删字典不删 demo）。
- **交付**（后端零改动，`.openspec.yaml skip_specs: true`）
  - `InsightPage.tsx`：删除 `next[p1..c4]`、`cause1/cause2`、`n1/n2` 写死字典与 `x.id==='pN'` 分支；归因卡/下一步只读 `x.semantics`；无 semantics → 归因区「模板未覆盖 / 引擎原文（metric·delta·question）」+ 下一步空态文案；头部固化「字段 → 来源 → 降级文案」盘点表（审查补强 2）
  - `data.ts`：o1/o2/c1–c4 补结构化 `semantics`（p1–p3 由 W1 已补）→ 9/9 全覆盖（审查补强 1）
  - 保留：AskBar / FollowupCard / InsightChart / 「加入行动回路|加入持续关注」/ 重新解释（F8/F11 不夹带）
- **测试证据**
  - `cd frontend && npm run build` ✅（tsc -b + vite，48 modules）
  - 静态断言：`grep -nE 'const (next|cause1|cause2|n1|n2)|causeA=sem|next\[x\.id\]' InsightPage.tsx` 无命中（exit=1）；`grep -c "semantics: { causeA" src/data.ts` = 9；`grep -cE '<AskBar|<FollowupCard|InsightChart'` = 4
  - `PATH=/Users/tomara/miniforge3/bin:$PATH python3 -m tests.run_all` → **ALL GREEN**（后端零改动回归确认）
  - 浏览器双态冒烟（用户确认）：在线=归因/下一步与引擎 semantics 一致；停后端=与改动前 demo 观感一致、无假值（含无 semantics 降级抽查）
- **反思**：与 W1 同源——Insight 的"下一步/归因"此前靠 id 字典，真实数据集（无 p1…c4）会显示与该洞察无关的文案；收敛为"引擎 semantics 或显式缺省"是唯一诚实解。删字典必须把等价内容同步下沉离线镜像并做 9/9 校验，否则离线演示退化。F8/F11 继续单列，不因"顺手"扩范围。

## 迭代 42（OpenSpec change `agent-tool-layer`，workflow.md §6.2）：Agent 工具层 / 知识检索 / 运行记录 / Dora Chat SSE

- **时间**：2026-09-10
- **目标**：为 `docs/workflow.md`（类 Dify agent 工作流）补齐后端能力四项——只读指标工具、知识检索（RAG references）、Agent 运行记录（可回放）、Dora Chat SSE；全部**确定性**（无 LLM 也可跑、可断言）。
- **交付**（新建能力 spec `business-agent` +4 需求；run_all 7 段 → **8 段**）
  - 工具层：`GET /api/tools/query_metric?metric_key=&dimension=&limit=`（只读；未知指标返回空数组不 404）+ `Repository.list_metric_series`
  - RAG 检索：`POST /api/knowledge/search {query,types?,top_k?}`（词法打分 top-k + 可回溯 references；空命中 `empty=true`）+ `Repository.search_knowledge`
  - 运行记录：新表 `agent_run`（events 用 JSON）+ `POST/GET /api/agent/runs`、`POST /api/agent/runs/{id}/events` + Repository 五方法（含 delete_all 供测试自清）
  - 问答流：`POST /api/dora/chat` SSE（`thought_step/tool_call/evidence/answer/done`；落一条 `agent_run(trigger=chat)`；答案只由引擎字段 + 模板语义 + 检索引用组装）
  - 门禁：新增 `tests/agent_check.py`（4 节）并纳入 `run_all`
- **测试证据**
  - `agent_check OK（工具层 / 知识检索 / 运行记录 / 问答流 4 节）`：query_metric margin 命中 / 未知指标空数组；search 命中 `references[0].source_id` 可回溯 + 无关词 `empty=true`；runs create/get/append + 不存在 404；chat 5 类事件 + run 落库 + **数字锁**（答案数字 ⊆ 引擎 payload）
  - `run_all: ALL GREEN`（**8 段**，含 agent_check；golden 9 不变）；`openspec validate --changes` 通过（`agent-tool-layer` 1/1）
  - 数字锁首版即拦下真问题：模板中的「连续 3 天」与「1)2)3)」编号越界 → 改为中文数字/无编号（红线生效）
- **反思**：Agent 能力的"地基"不是 LLM，而是**可调用、可断言、可回放**的后端面——工具只读且未知即空、检索必须回填可回溯引用、运行轨迹落库、答案数字只能来自引擎；有这四项，Dify 工作流才"可验证"而不只是"能跑"。RAG 先词法（零依赖、确定性），向量化另立；Chat 先模板语义（与 `reasonSource=template` 对齐），真实多 Agent 编排交给 Dify 侧。环境侧：本机 PG 容器此前已停，本轮 `docker compose up -d` 恢复，后端改 `--host 0.0.0.0`（Dify 容器可访问，见 workflow.md §11.0）。

## 迭代 43（OpenSpec change `insight-panel-polish`，持续优化路线 §2「洞察展示收敛」）：判断列去复述 + AI 判断接入 + 抽屉/图表/脉搏页收口

- **时间**：2026-09-11
- **目标**：把用户实测发现的「判断列复述下方『下一步建议』面板」收口成一个 change，并同批收拢本轮**全部**展示层改动（5 个前端文件），一次性消除「同屏同一信息出现两次」及若干排布缺陷。
- **交付**（后端零改动，`.openspec.yaml` `skip_specs: true`；归档 `openspec/changes/archive/2026-09-11-insight-panel-polish`）
  - `doraApi.ts`：新增 `askDora(question,{insightId,page})` —— `POST /api/dora/chat` SSE 逐事件解析（`answer`/`thought_step`/`done`）+ 20s 超时 `AbortController`；`MODE==='mock'` 直接抛错（由调用方降级）。
  - `InsightPage.tsx`：判断列接入 Dora AI 判断（loading「正在结合证据判断…」/ 成功用 `answer.text` 原文 / 失败或离线降级引擎 `semantics` + 「AI 暂不可用，以上为引擎语义兜底」）；`belongsElsewhere` 行归类 + `ownRest`（**展开也不带出**归类行）；左栏「历史先例」行三态（命中 `标题（code）` / 暂无 `.ev-none` / 失败不渲染）；事实·判断两栏卡片等高 + 置信度贴底（`.verdict-conf{margin-top:auto}`）；洞察列表折叠抽屉（300px ↔ 46px）；归因卡两列；头部字段盘点表补「判断列」来源与去重规则。
  - `InsightChart.tsx`：5 处 `preserveAspectRatio` → `xMidYMid meet`（等比）；纵轴刻度按 `tickStep=(mx-mn)/5` 定小数位 + `-0` 归一（消除 `20%/20%` 重复标签）。
  - `PulsePage.tsx` + `styles.css`：专家团 chips 可点切换（`capabilities['专家团']` + `autoExpertNotes`，默认第一位）；信号板/卡片收口（数值与变化不换行、按钮右对齐）；移除「持续关注」卡「?」note（`.summary-card .q` 规则删除并留注释）。
  - **P013 修复（apply 期间发现）**：`className="judge-col facts"` 撞上遗留全局 `.facts{margin-top:9px;gap:8px}` → 两栏高度 255/264 不等；类名收敛为 `judge-facts` 后 239/239 等高。
- **测试证据**
  - `cd frontend && npm run build` → `✓ 47 modules transformed / ✓ built`（tsc -b + vite）
  - `PATH=/Users/tomara/miniforge3/bin:$PATH python3 -m tests.engine_check` → `engine self-check OK`（pulse 3/2/4/4、9 insights 值不变）；`… -m tests.run_all` → **ALL GREEN**（engine/ingest/reasoning/golden 9/e2e 4/watch/action/agent）→ 证明**后端零改动**
  - CDP `/tmp/dora_polish_verify.mjs` → **17/17 PASS**：结论句逐字 = chat 首句；`dup={"next":false,"prev":false,"ref":false,"fact":false}`；抽屉 300→46（detail 1072→1326）再点复原 300；两栏 `496×239 / 496×239`；归因卡 `229px 229px`；三视口圆点宽高比 `{"1400":1,"1680":1,"1920":1}`；纵轴 `["18.2%","18.7%","19.3%","19.9%","20.5%","21.0%"]`；chips 4 个可切换；`.summary-card.watch .q` = false；`signal-row=6` 全部含查看按钮；脉搏页 `scrollWidth-clientWidth=0`
  - CDP `/tmp/dora_judge_verify.mjs`（三 tab + 展开态）→ `[PASS] 三 tab 判断列均与其它区块无重复；下一步建议/先例各归其位`
  - CDP `/tmp/dora_offline_check.mjs`（`Fetch.failRequest` 拦截 `/api/dora/chat` 造故障）→ **6/6 PASS**：降级为引擎语义、显示兜底 note、仍不复述、不伪造「历史先例」、下方建议面板不丢、置信度仍在
  - 截图：`/tmp/dora_shots/{panel,judge,drawer_closed,pulse_chips,pulse_signals,offline_fallback}.png`（1680 宽 2x）
  - `openspec validate insight-panel-polish --strict` ✅ / `python3 -m tests.check_docs` → ALL GREEN
- **反思**：① 去复述的正解是**换位**（每类信息各归其位、零丢失），不是删内容；② 「看起来对」≠「像素对」——等高必须**成对量像素**，只断言 `align-items:stretch` 会漏掉 `.facts` 撞名蚕食 9px 这类静默失效（P013）；③ 展示层改动一旦并入「本轮全部 UI 改动」，propose 三件套必须**同步扩写**（范围 / Impact / 验证 / D5–D8），否则归档时说不清这批未提交 diff 的归属——本轮 propose 扩写是补做的，属**流程偏差**，已在 tasks 1.2 显式承认；④ 用户提出「判断应是 严重等级→为什么→后果」暴露真实缺口：`tag` 的「高/中影响」是 `INSIGHT_TEMPLATES` 写死文案、**后果字段与 chat 意图都不存在** → 已登记为下一 change `insight-judgment-structure`（含后端 `severity`/`consequence` + `business-engine` spec delta + 前端三段式，不再 `skip_specs`），本 change 不塞无来源字段（守 D004 红线）。

## 迭代 44（OpenSpec change `action-loop-timeline`，持续优化路线 §2「行动回路页收口」）：档案列表二值化 + 步骤时间线 + 每步负责专家落库 + 经验收敛到知识库

- **时间**：2026-09-11
- **目标**：一次做完用户提出的「行动回路页收口」——档案列表对齐洞察列表（状态二值 pill + 计数 + 执行中在前）、「学习经验」移出 Action 页收敛到知识库、机会/问题步骤区改时间线（可展开收起 + 控件右置 + **专家落到每一步并落库** + 每步数据定位）。
- **交付**（归档 `openspec/changes/archive/2026-09-11-action-loop-timeline`；spec 增量 1 条 ADDED + 3 条 MODIFIED 合入 `openspec/specs/business-action/spec.md`）
  - 后端：`ActionStep.experts`（JSON，缺省 `[]`）+ `db.py` 幂等补列清单（`ALTER TABLE … ADD COLUMN IF NOT EXISTS`，旧行零迁移、不伪造专家）+ `repository.set_action_step_experts`（去重保序、写 `updated_at`、seq 不存在返回 `None`）+ `action/flow.set_step_experts`（复用 `_get` 冻结 + `_step` 404）+ `schemas.StepExpertsRequest` + `POST /api/action/cases/{case_id}/steps/{seq}/experts`（整体设置、幂等；空名/非列表/超 8 → 400/422、case 404、seq 4xx、resolved 冻结 4xx、返回 `{ok, case}`）。
  - 前端：`ActionPage` 列表行重写为洞察列表形态（`Tag(kind)` + `.list-status` 二值 pill + code + 标题 + 来源；执行中在前 + 头部「执行中 N · 已归档 M」）；步骤区改时间线（**复用既有死 CSS** `.timeline/.tl/.tldot/.tl-body/.tl-head`，新增仅带前缀的 `.tl-actions`/`.tl-more`；`openSteps` 默认展开当前步、换档案重置）；推进/展开控件移入 `.tl-actions`（右置、折叠态仍可见）；每步专家 chips（`.expert-chip.user`）+ 候选池（`capabilities['专家团']` ∪ 档案级「系统建议」）、添加/移除走整体设置端点、失败只提示不乐观更新；每步 `.step-evidence` + 「定位数据 →」（空则「暂无定位说明」）；删 Action 页经验块与 `listLessons` 拉取（`archiveLesson` 保留，toast 指向知识库）；`KnowledgePage` 处理过程改真折叠；`types.ts` 增 `CaseStep.experts?`、`doraApi.setStepExperts`（离线抛不可用标记）。
  - 离线演示分支：列表 pill 显示「离线演示」+ 说明「步骤执行 / 专家分配需后端在线」，不渲染时间线/专家、不伪造「执行中/已归档」。
- **测试证据**
  - `cd backend && python3 -m tests.action_check` → `action_check OK（第 1–7 节 + 生命周期不变式：…/步骤负责专家/无孤儿 全绿）`；新节 `[experts] 每步负责专家：往返/去重/幂等/整体替换/上限/冻结/404 全绿`、`[experts] HTTP 200/400/404/422 断言已执行（后端在线）`
  - `python3 -m tests.run_all` → `run_all: ALL GREEN`（8 段，exit=0；golden 9 不变）
  - curl（`--noproxy '*'`）：`POST …/cases/p1/steps/1/experts {"experts":["经营分析专家"]}` → `ok=True | steps[1].experts=['经营分析专家'] | HTTP200`；9 个专家 → 400、`[""]` → 400、非列表 → 422、不存在 case → 404、不存在 seq → 400、复位 `[]` → 200
  - `python3` 仓储探针（临时档案，跑完即删）：重复项去重保序 `['财务专家','销售专家']`、不存在 seq → `None`、清空 → `[]`
  - `cd frontend && npm run build` → `✓ 47 modules transformed / ✓ built in 698ms`
  - CDP `/tmp/dora_action_verify.mjs`（后端在线）→ **28/28 PASS**：6 行档案 + 头部「执行中 5 · 已归档 1」+ pill 二值 + 已归档排末尾；节点数=步骤数 5、默认仅展开当前步、序号 1..5；控件右置（标题 `left=630` / 控件组 `left=1459`，`top` 差 5px）+ 折叠态控件组可见 + 按钮 `["完成","受阻","收起"]`；专家添加后 chip 与接口 `steps[2].experts` 一致、移除后空态 + 接口 `[]`；有 evidence 打开证据链抽屉、空 evidence 呈现「暂无定位说明」；resolved 档案冻结推进（只有「继续观察」、无「开始/完成」）；沉淀 toast 含「知识库」；知识库处理过程默认收起 → 点击可见全文
  - CDP `/tmp/dora_action_offline.mjs`（`Fetch.failRequest` 拦截 `/api/action/cases*`）→ **4/4 PASS**：pill 全为「离线演示」+ 文案命中 + 不伪造计数头 + 无 `.tl`/专家 chip
  - 截图：`/tmp/dora_shots/{action_timeline,knowledge_fold,action_offline}.png`
  - `openspec validate --specs` → `Totals: 7 passed, 0 failed`；`openspec validate action-loop-timeline --strict` → valid（同步后提示 ADDED 已存在=预期，故归档用 `openspec archive --skip-specs` 完成移动）
- **反思**：① 「专家团落到每一步」表面是 UI 小改，实质是**契约问题**——所以先立持久化面（列 + 端点 + 幂等 + 冻结校验），前端才有真数据可渲染，也才谈得上「不乐观更新」；② 复用「已存在但无引用的死 CSS」比新写样式更省，但**前提是逐条核对类名语义**（P013 的教训：通用类名撞名会静默改布局）；③ 需求里的「状态二值化」刻意只在展示层做——改内部枚举会牵动状态机契约、门禁与历史行，收益为零；④ 门禁前置状态必须显式对齐（DB seed + 清非 seed 档案），否则 `engine_check`/`action_check` 会报看起来像回归的数据问题（新记 P014）；⑤ 「离线不伪造」是复用 D003 红线的结果：演示镜像没有 `status`，就不画状态、只诚实标注。

## 迭代 45（OpenSpec change `watch-delegate-flow`，持续优化路线 §2「持续关注（业务目标委托）页收口」）：委托卡片生命周期回显 + 「Dora 接到委托后会做什么」状态驱动 + 操作列固定宽

- **时间**：2026-09-11
- **目标**：一次做完用户提出的两件事——① 右侧「Dora 接到委托后会做什么」从 `WatchPage.tsx` 写死的静态四步改为**随真实委托状态推进的流程**；② 表格「操作」列加宽使「检查/暂停(恢复)/删除」三按钮并排不折行。红线：不嗅探 `logic`/`value` 字符串（D003/D004）、离线不伪造、既有消费方零改动。
- **交付**（归档 `openspec/changes/archive/2026-09-11-watch-delegate-flow`；spec 增量 1 条 MODIFIED 合入 `openspec/specs/business-watch/spec.md`）
  - 后端：`routers.py::_watch_card` 增量 3 个 camelCase 字段——`intent`（解析结果原样）、`lastCheckedAt`（`last_checked_at`）、`lastEvent`（`{kind, summary, triggeredAt}` 或 `None`）；复用函数内**已取出**的 `intent`/`events`/`last`，零新增查询、既有 9 键取值不变。
  - 前端：新增 `features/watch/delegateFlow.ts`（`buildDelegateFlow({intent,target,online})` → `{state,stateLabel,steps[],hint}`，含 4 步语义映射 + 头部 pill 同源推导；同时导出 `conditionText/frequencyText/shortAt/metricNote` 供组件共用，消除 WatchPage 内联条件/频率文案两份）；`WatchPage.tsx` 面板改由派生结果渲染（保留 4 步标题骨架与「自动完成」Tag，新增头部状态 pill + 当前委托行 + hint）、`focusId` 跟随本会话创建的委托（否则 `targets[0]` 标「最近委托」）、离线时面板不声称有当前委托（`当前委托 · 未连接后端`）；`types.ts` 增 `WatchEventCard` + `WatchTargetCard.intent?/lastCheckedAt?/lastEvent?`；`doraApi.listWatch(onFallback?)` 显式上报「走了镜像兜底」；`styles.css` 第 4 列 `0.45fr` → `208px`、`.wopts{flex-wrap:nowrap}`、`.rowbtn{white-space:nowrap}`、移动端 `.wopts` 独占一行左对齐。
- **测试证据**
  - `cd backend && python3 -m tests.watch_check` → `watch_check OK（第 1 节 CRUD + 第 2 节 解析 + 第 3 节 评估器 + 第 4 节 升级 E2E 全绿）`（第 4 节新增卡片断言三组：创建卡片 `intent.metric_key=='east_orders'`+`lastCheckedAt` 非空+`lastEvent.kind=='change'`；升级卡片 `lastEvent.summary == f"已升级：{/insights/e2.trigger}"`；「超过 999999」委托 `lastEvent is None` 且 `lastCheckedAt` 非空）
  - `python3 -m tests.run_all` → `run_all: ALL GREEN`（8 段；`golden_check OK (9 insights matched baseline)` 不变）
  - curl（`--noproxy '*'`）`/api/watch` → 每卡新增三键、既有 9 键取值不变（`intent={margin,全国,streak_below×3}`、`lastCheckedAt=2026-09-11T10:36:31+00:00`、`lastEvent={escalate,'已升级：连续 4 天低于目标 18.5%'}`）
  - `cd frontend && npx tsc -b` exit=0；`npm run build` → `✓ 48 modules transformed / ✓ built in 524ms`
  - `delegateFlow` 纯函数断言（`npx esbuild src/features/watch/delegateFlow.ts --bundle --format=esm --outfile=/tmp/flow.mjs` + `node /tmp/flow_assert.mjs`）→ **24/24 PASS**（七态：无委托/仅理解/持续关注/命中/升级/暂停/离线 + 3 条补强：卡片 intent 回填、暂停但曾升级保留历史、离线仍按本地 parse 推进步骤 1/2）
  - CDP（临时驱动 `/tmp/cdp.mjs` + 用例，Chrome `--headless=new --remote-debugging-port=9333`）在线全流程单会话三快照 → 待命 → `已理解委托 · 待确认`（步骤 1 note `华东销售额 · 华东 · 连续 3 天下跌`、步骤 2 note `引擎口径 · east_orders（华东）`）→ 创建后 `已命中变化`（步骤 3 `数据更新时 · 最近检查 09-11 11:04`、步骤 4 note = 接口 `lastEvent.summary`）`[PASS]`；跨一次 20s 轮询前后快照 `same=true` `[PASS]`；八段交互回归覆盖 6 个动作（含 `开始持续关注` → `已升级`、`检查` → toast「命中升级（已回脉搏）」、`暂停` → 步骤 3 `已暂停 · 不评估`、`恢复` → 回 `进行中`、`删除` → 面板回「待命」+ 接口 0 条）`[PASS]`
  - CDP 离线分支（`BLOCK_URL='api/watch'` 用 `Fetch.failRequest` 拦截）→ `hasOfflineTag=true`、步骤 3/4「需连接后端」、无「持续检查中/已命中变化/已升级」伪造态 `[PASS]`
  - CDP 布局四视口（1440/1024/760/390）→ 每行三按钮 `top` 相同、`.wopts` 高 = 单行按钮高 36、`tableOverflow=0`、`body/docNoHScroll=true`、`.wlogic` 溢出 `[0,0,0,0]` `[PASS]`；截图 `/tmp/watch-shots/watch-1440.png`(1440×1123) / `watch-1024.png`(1024×1444) / `watch-390.png`(390×2305)
  - `openspec validate watch-delegate-flow --strict` → valid；`openspec validate --specs` → `7 passed`；`python3 -m tests.check_docs` → ALL GREEN
- **反思**：① **设计假设必须回代码验证**——D5 写的是「`load()` 落到 `catch` 即离线」，实测 auto 模式的服务层（`apiGet`）**内部兜底、从不抛错**，组件永远等不到 `catch`；改为由服务层 `listWatch(onFallback)` 显式上报（语义不变、且精确匹配「`/api/watch` 失败」这一触发条件）——这是 apply 期纠正 propose 的典型（已记 P015）。② 状态驱动面板的价值在**同源**：pill 与四个步骤都从同一次 `_watch_card` 计算派生，才敢断言「不会 pill 说 A、步骤说 B」；反过来，卡片增量字段比消费 snake_case 详情端点更小改动（D004）。③ 面板跟着「当前委托」而不是全局，离线时**连「当前委托」都不声称**——诚实性是从「镜像没有 `status` 就不画状态」推到底的自然结论。④ 固定列宽换来「三按钮永远同排」，代价是 1024–760px 之间逻辑列更窄（换行不溢出，已断言）；移动端覆盖**必须写在基样式之后**否则同权重被覆盖成死 CSS（P015）。⑤ 并发跑两个 CDP 驱动争用同一 page target 会让截图/量测互相污染（两张截图 md5 相同）——验证脚本也要串行（P015）。


## 迭代 46（OpenSpec change `insight-judgment-structure`，持续优化路线 §2「洞察判断结构化」）：判断列三段式（引擎严重等级 + 确定性后果外推 + chat 后果意图）

- **时间**：2026-09-11
- **目标**：把洞察页判断列从「一段散文」升为**三段式**——① 严重等级（由数据算，不是模板文案）② 为什么发生（沿用 chat 散文）③ 可能后果（引擎确定性外推）。红线：判级/外推**不引入 LLM 与快照外数字**（数字锁，D004/D011）；`tag` 文案与既有消费者零改动；离线/降级路径三段仍完整。
- **交付**（归档 `openspec/changes/archive/2026-09-11-insight-judgment-structure`；spec 增量 3 条 ADDED：`business-engine` ×2 + `business-agent` ×1）
  - 后端（`app/engine/engine.py`）：新增 `SEVERITY_WEIGHTS`（权重集中一处）+ `_severity_measures(sig, snap)`（把每条洞察的判级量——偏离幅度/距阈值/持续/影响面——从**同一份快照**取出，供文案与门禁共用）+ `_trend_score`/`_threshold_score`（`max(趋势, 阈值)` 取较大者，避免同一事实重复计分）+ `_severity_drivers` + `_severity`（`score = base + max + 持续 + 影响面 + 归因面`，切点 75/55）+ `_consequence_of`/`_consequence`（10 个洞察分支 + `c2` 定性分支）；`build_insights()` 每算每出下发 `severity`/`consequence`（不入库、不进 reasoning 缓存）。
  - 后端（`app/routers.py`）：`CHAT_INTENT_RULES` 新增 `consequence`（后果/不处理/会怎样/会不会/风险/影响面，**排在 `handle` 之前**——「不处理」含「处理」子串）；`_compose_answer` 新增分支产出 `严重等级：…`/`可能后果：…` 两段（文本只由引擎字段组装）。
  - 后端（`app/schemas.py`）：`InsightFactor`/`InsightSeverity`/`InsightConsequence` + `InsightSummary.severity/consequence`——`GET /api/insights/{id}` 走字段白名单，不扩就静默丢新字段（apply 期发现）。
  - 门禁：`tests/engine_check.py` 新增 severity/consequence 契约、9 条等级分布、`c1 ≤ e2`（同一快照构造跌破对照信号注入）、敏感性单调、数字锁回溯（drivers 数字 ⊆ payload−severity ∪ 快照 ∪ measures 派生量）、两次运行全等；`tests/agent_check.py` 第 4 节新增后果意图两段 + why/evidence 不回归 + 数字锁（把一次性 curl 固化为常驻门禁）。
  - 前端：`types.ts` 增 `InsightSeverity`/`InsightConsequence`（`Insight` 上可选）；`InsightPage.tsx` 段①（`.sev-block`：等级徽章 + `score/100` + `rule` + `drivers` chips）、段③（`.consq-block`：`summary` + 前提/窗口 + `impacts` chips）落在判断列顶部/置信度之上，`belongsElsewhere` 增 `/^严重等级[：:]/`、`/^可能后果[：:]/`；`styles.css` 增 `.sev-*`/`.consq-*`（含 high/medium/low 三色徽章）；`data.ts` 增 `engineJudgment` 常量表（9 条镜像的 severity/consequence 按引擎同口径）+ 挂载循环 + 来源/同步注释。

## 测试证据

- **后端**：`cd backend && python3 -m tests.engine_check` → `engine self-check OK`（`pulse = {'problems': 3, 'opportunities': 2, 'changes': 4, 'watching': 4}` 不变；等级分布 `p1 high 76 / p2 74·p3 64·o1 59·o2 67 medium / c1 37·c2 35·c3 49·c4 49 low`）；`python3 -m tests.agent_check` → `[4] dora/chat OK（5 类事件 + run 落库 + 数字锁 + 后果意图两段）`；`python3 -m tests.run_all` → **ALL GREEN**（engine/ingest/reasoning/golden/e2e/watch/action/agent 8 段；`golden_check OK (9 insights matched baseline)`）
- **端点**（`curl --noproxy '*'`）：`/api/insights?type=problem` 每条含 `severity{level,score,rule,drivers,basis}` + `consequence{summary,horizon,condition,impacts,basis}`；`/api/insights/p1` 键集新增 `severity`/`consequence`；`POST /api/dora/chat {"question":"这条洞察不处理会怎样？","insight_id":"p1"}` → `intent=consequence`，answer 首两段 = `严重等级：高 76 分 · 偏离目标 5.4%、距目标线 已越线、连续低于目标 4 天、受影响供应商 1 家、归因因素 2 项` / `可能后果：若不干预，利润率将延续 5.4% 的偏离幅度（当前 18.1%，目标 18.5%），成本端压力继续放大（若成本端未干预；观察窗口 4 天）`；answer 数字未回溯列表 `[]`
- **前端**：`npx tsc -b` exit=0；`npm run build` → `✓ 48 modules transformed / ✓ built in 537ms`；镜像一致性脚本（esbuild bundle + `node /tmp/compare_judgment.mjs`）→ `镜像 9 条 / 引擎 9 条；不一致 0 处`
- **端到端（CDP）**：`CDP_PORT=9222 node /tmp/dora_judge3_verify.mjs` → **22/22 PASS**：三 tab（problem/opportunity/change）× 段①等级徽章+依据、段③后果（若… + 窗口 + 影响项）、去复述（含展开态 `dup` 全 false）、两栏等高（496×428 / 496×342 / 496×383）+ 置信度贴底（`confGap 13 = padding-bottom 12 + border 1`）、图表 `xMidYMid meet`；抽屉回归 `46 ↔ 300`；视口 1440/1024/390 三段齐全 + `overflow=0`；离线（`Fetch.failRequest` 拦 `/api/dora/chat`）段①/段③仍在、段②降级 + `AI 暂不可用，以上为引擎语义兜底`；截图 `/tmp/dora_shots3/judge3_{panel,cols,offline}.png`
- **文档/spec**：`python3 -m tests.check_docs` ALL GREEN；`openspec validate insight-judgment-structure --strict` valid；spec 增量合入后 `openspec validate --specs` → `Totals: 7 passed, 0 failed`

## 反思

1. **验收命令 = 会执行的假设，propose 期就要跑**：本轮 3 条命令中 2 条与实况不符——`i['e2']` 在当前种子下不存在（`c1`/`e2` 是 `east_orders` 同一信号的互斥分支，design 与 tasks 各写了一次）；`/api/insights/{id}` 走 `InsightSummary` 字段白名单，不扩 `schemas.py` 就静默丢字段。固化为 **P016**（互斥分支不能并列断言 → 改注入式断言）。
2. **判级要可解释而非「看起来精确」**：把权重收敛到 `SEVERITY_WEIGHTS`、把依据摊成 `drivers` 挂在卡片上，任何「为什么是 76 不是 80」都能逐项对账；代价是启发式而非财务口径——有意取舍，退出条件写在 D040（要行业可配置就把权重提到规则参数）。
3. **数字锁要能自动守，且不能自证**：让派生量由 `_severity_measures` 显式返回，`engine_check` 只允许「文本数字 ⊆ payload ∪ 快照 ∪ measures」；`drivers` 断言刻意排除 `severity` 自身（否则 payload 自带 drivers = 自证）。硬编码数字必被抓——首轮即抓到镜像 `o2` 文案一处空格差异。
4. **诚实性边界写进注释，不靠记忆**：离线镜像其余数值仍是旧演示快照（`18.7%/↓2.1%`）而新字段取自当前引擎（`18.1%/↓5.4%`）→ 离线卡片可能同屏两套数字；按 D4 取舍（离线/在线判级一致优先）执行，并把「未同步」写进 `data.ts` 注释 + D040 实测补充 + backlog 候选，避免下轮当 bug 重复讨论。
5. **量了还要量对基准**：CDP「置信度贴底」按 `confGap == 0` 断言得到 3 个假红，正确基准是卡片 `padding-bottom + border`（13px）——与 P013「必须量像素」同族，断言基准要从计算样式取。


> 每个 backlog 项的落地形态是 **`openspec/changes/`** 下的一个 change（propose → apply → archive）。
> 迭代记录仅追加在本文各"迭代 N"节；待办状态一律去路线图查看/更新。
